_ do [
    [major, minor, patch] = air.version,
    major = _ syntax.generate #major,
    minor = _ syntax.generate #minor,
    patch = _ syntax.generate #patch,
    "." text.join [#major, #minor, #patch]
]
