_ do [
    version = _ import .language.version,
    generate = _ import .syntax.generate,
    join = _ import .text.join,
    [major, minor, patch] = version,
    major = _ generate major,
    minor = _ generate minor,
    patch = _ generate patch,
    "." join [major, minor, patch]
]
