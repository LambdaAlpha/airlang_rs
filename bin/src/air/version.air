_ do [
    version = _ import _language.version,
    generate = _ import _language.syntax.generate,
    join = _ import _text.join,
    [major, minor, patch] = version,
    major = _ generate major,
    minor = _ generate minor,
    patch = _ generate patch,
    "" join ["v", major, ".", minor, ".", patch]
]
