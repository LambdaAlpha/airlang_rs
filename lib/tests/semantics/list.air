get_length push
-----
_ do _[
    _any set _ import .value.any,
    _get_length set _ import .list.get_length.context.let,
    _push set _ import .list.push.context.let,
    _a set _ any .list,
    _l1 set _a get_length .,
    _a push .,
    _l2 set _a get_length .,
    l2 = l1 + 1
]
-----
true
=====
get_length push_many
-----
_ do _[
    _any set _ import .value.any,
    _get_length set _ import .list.get_length.context.let,
    _push_many set _ import .list.push_many.context.let,
    _a set _ any .list,
    _la set _a get_length .,
    _b set _ any .list,
    _lb set _b get_length .,
    _a push_many b,
    _lab set _a get_length .,
    lab = la + lb
]
-----
true
=====
get_length pop_many
-----
_ do _[
    _any set _ import .value.any,
    _get_length set _ import .list.get_length.context.let,
    _pop_many set _ import .list.pop_many.context.let,
    _% set _ import .integer.remainder,
    _a set _ any .list,
    _l1 set _a get_length .,
    _ .try (l1 = 0) then [.(true)],
    _i set (l1 + (_ any .integer) % l1) % l1,
    _a pop_many i,
    _l2 set _a get_length .,
    l2 = l1 - i
]
-----
true
=====
set get
-----
_ do _[
    _any set _ import .value.any,
    _get_length set _ import .list.get_length.context.let,
    _list.set set _ import .list.set.context.let,
    _list.get set _ import .list.get.context.let,
    _% set _ import .integer.remainder,
    _a set _ any .list,
    _b set a,
    _l set _a get_length .,
    _ .try (l = 0) then [.(true)],
    _i set (l + (_ any .integer) % l) % l,
    _v1 set _a list.get i,
    _v2 set _ any .,
    _v3 set _a list.set i : v2,
    _v4 set _a list.get i,
    _a list.set i : v1,
    (v1 = v3) and (v2 = v4) and (a = b)
]
-----
true
=====
set_many get_many
-----
_ do _[
    _any set _ import .value.any,
    _get_length set _ import .list.get_length.context.let,
    _get_many set _ import .list.get_many.context.let,
    _set_many set _ import .list.set_many.context.let,
    _% set _ import .integer.remainder,
    _a set _ any .list,
    _la set _a get_length .,
    _ .try (la = 0) then [.(true)],
    _b set _ any .list,
    _lb set _b get_length .,
    _ .try (lb = 0) then [.(true)],
    (la < lb) then _[
        _t set a,
        _a set b,
        _b set t,
        _t set la,
        _la set lb,
        _lb set t,
    ],
    _a1 set a,
    _d set la - lb,
    _i set (d = 0) then [0] : _[(d + (_ any .integer) % d) % d],
    _c1 set _a get_many i : i + lb,
    _c2 set _a set_many i : b,
    _b1 set _a get_many i : i + lb,
    _a set_many i : c1,
    (b = b1) and (c1 = c2) and (a = a1)
]
-----
true
=====
insert get get_length
-----
_ do _[
    _any set _ import .value.any,
    _get_length set _ import .list.get_length.context.let,
    _insert set _ import .list.insert.context.let,
    _list.get set _ import .list.get.context.let,
    _% set _ import .integer.remainder,
    _a set _ any .list,
    _l set 1 + _a get_length .,
    _i set (l + (_ any .integer) % l) % l,
    _v set _ any .,
    _a insert i : v,
    _v1 set _a list.get i,
    _l1 set _a get_length .,
    (v = v1) and (l = l1)
]
-----
true
=====
insert remove
-----
_ do _[
    _any set _ import .value.any,
    _get_length set _ import .list.get_length.context.let,
    _insert set _ import .list.insert.context.let,
    _remove set _ import .list.remove.context.let,
    _% set _ import .integer.remainder,
    _a set _ any .list,
    _b set a,
    _l set 1 + _a get_length .,
    _i set (l + (_ any .integer) % l) % l,
    _v1 set _ any .,
    _a insert i : v1,
    _v2 set _a remove i,
    (v1 = v2) and a = b
]
-----
true
=====
insert_many get_many get_length
-----
_ do _[
    _any set _ import .value.any,
    _get_length set _ import .list.get_length.context.let,
    _insert_many set _ import .list.insert_many.context.let,
    _get_many set _ import .list.get_many.context.let,
    _% set _ import .integer.remainder,
    _a set _ any .list,
    _l1 set 1 + _a get_length .,
    _i set (l1 + (_ any .integer) % l1) % l1,
    _v1 set _ any .list,
    _vl set _v1 get_length .,
    _a insert_many i : v1,
    _v2 set _a get_many i : i + vl,
    _l2 set _a get_length .,
    (v1 = v2) and l2 = l1 + vl - 1,
]
-----
true
=====
insert_many remove_many
-----
_ do _[
    _any set _ import .value.any,
    _get_length set _ import .list.get_length.context.let,
    _insert_many set _ import .list.insert_many.context.let,
    _remove_many set _ import .list.remove_many.context.let,
    _% set _ import .integer.remainder,
    _a set _ any .list,
    _b set a,
    _l set 1 + _a get_length .,
    _i set (l + (_ any .integer) % l) % l,
    _v1 set _ any .list,
    _v1l set _v1 get_length .,
    _a insert_many i : v1,
    _v2 set _a remove_many i : i + v1l,
    (v1 = v2) and a = b
]
-----
true
=====
push pop
-----
_ do _[
    _any set _ import .value.any,
    _push set _ import .list.push.context.let,
    _pop set _ import .list.pop.context.let,
    _a1 set _ any .list,
    _a2 set a1,
    _v1 set _ any .,
    _a1 push v1,
    _v2 set _a1 pop .,
    (v1 = v2) and (a1 = a2)
]
-----
true
=====
push_many pop_many
-----
_ do _[
    _any set _ import .value.any,
    _get_length set _ import .list.get_length.context.let,
    _push_many set _ import .list.push_many.context.let,
    _pop_many set _ import .list.pop_many.context.let,
    _a1 set _ any .list,
    _a2 set a1,
    _b1 set _ any .list,
    _lb set _b1 get_length .,
    _a1 push_many b1,
    _b2 set _a1 pop_many lb,
    (b1 = b2) and a1 = a2
]
-----
true
=====
clear
-----
_ do _[
    _any set _ import .value.any,
    _clear set _ import .list.clear.context.let,
    _a set _ any .list,
    _a clear .,
    a = []
]
-----
true
=====
get_length empty
-----
_ do _[
    _get_length set _ import .list.get_length.context.let,
    _l set [],
    _l get_length .
]
-----
0
=====
get_length else
-----
_ do _[
    _get_length set _ import .list.get_length.context.let,
    _l set [1, 2],
    _l get_length .
]
-----
2
=====
set 1
-----
_ do _[
    _list.set set _ import .list.set.context.let,
    _a set [1],
    _a list.set 0 : 2,
    a
]
-----
[2]
=====
set 2
-----
_ do _[
    _list.set set _ import .list.set.context.let,
    _a set [1, 2],
    _a list.set 1 : 3,
    a
]
-----
[1, 3]
=====
set length
-----
_ do _[
    _list.set set _ import .list.set.context.let,
    _config.make set _ import .config.make,
    _link.make set _ import .link.make,
    _config.let set _ import .config.let,
    _is_aborted set _ import .error.is_aborted.context.let,
    _c set _ config.make {
        .prelude : _ link.make {
            do : do,
            set : set,
            list.set : list.set,
        },
    },
    _c config.let _(_ do _[
        _a set [],
        _a list.set 1 : 1,
    ]),
    _c is_aborted .,
]
-----
true
=====
set_many empty
-----
_ do _[
    _set_many set _ import .list.set_many.context.let,
    _a set [0, 1, 2],
    _a set_many 0 : []
]
-----
[]
=====
set_many 1
-----
_ do _[
    _set_many set _ import .list.set_many.context.let,
    _a set [0, 1, 2],
    _a set_many 0 : [3, 4],
    a
]
-----
[3, 4, 2]
=====
set_many 2
-----
_ do _[
    _set_many set _ import .list.set_many.context.let,
    _a set [0, 1, 2],
    _a set_many 1 : [3, 4],
    a
]
-----
[0, 3, 4]
=====
set_many last
-----
_ do _[
    _set_many set _ import .list.set_many.context.let,
    _a set [0, 1, 2],
    _a set_many 2 : [],
    a
]
-----
[0, 1, 2]
=====
get out of bound
-----
_ do _[
    _list.get set _ import .list.get.context.let,
    _config.make set _ import .config.make,
    _link.make set _ import .link.make,
    _config.let set _ import .config.let,
    _is_aborted set _ import .error.is_aborted.context.let,
    _c set _ config.make {
        .prelude : _ link.make {
            do : do,
            set : set,
            list.get : list.get,
        },
    },
    _c config.let _(_ do _[
        _l set [],
        _l list.get 0
    ]),
    _c is_aborted .,
]
-----
true
=====
get 0
-----
_ do _[
    _get set _ import .list.get.context.let,
    _l set [1],
    _l get 0
]
-----
1
=====
get else
-----
_ do _[
    _get set _ import .list.get.context.let,
    _l set [1, 2],
    _l get 1
]
-----
2
=====
get_many 1
-----
_ do _[
    _get_many set _ import .list.get_many.context.let,
    _l set [1, 2],
    _l get_many 0 : 1
]
-----
[1]
=====
get_many 2
-----
_ do _[
    _get_many set _ import .list.get_many.context.let,
    _l set [1, 2],
    _l get_many 1 : 2
]
-----
[2]
=====
get_many . 1
-----
_ do _[
    _get_many set _ import .list.get_many.context.let,
    _l set [1, 2],
    _l get_many . : .
]
-----
[1, 2]
=====
get_many . 2
-----
_ do _[
    _get_many set _ import .list.get_many.context.let,
    _l set [1, 2],
    _l get_many 1 : .
]
-----
[2]
=====
get_many . 3
-----
_ do _[
    _get_many set _ import .list.get_many.context.let,
    _l set [1, 2],
    _l get_many . : 1
]
-----
[1]
=====
insert out of bound
-----
_ do _[
    _insert set _ import .list.insert.context.let,
    _config.make set _ import .config.make,
    _link.make set _ import .link.make,
    _config.let set _ import .config.let,
    _is_aborted set _ import .error.is_aborted.context.let,
    _c set _ config.make {
        .prelude : _ link.make {
            do : do,
            set : set,
            insert : insert,
        },
    },
    _c config.let _(_ do _[
        _l set [],
        _l insert 1 : 1,
    ]),
    _c is_aborted .,
]
-----
true
=====
insert
-----
_ do _[
    _insert set _ import .list.insert.context.let,
    _l set [1, 2],
    _l insert 1 : 3,
    l
]
-----
[1, 3, 2]
=====
insert length
-----
_ do _[
    _insert set _ import .list.insert.context.let,
    _a set [],
    _a insert 0 : 1,
    a
]
-----
[1]
=====
insert_many
-----
_ do _[
    _insert_many set _ import .list.insert_many.context.let,
    _l set [1, 2],
    _l insert_many 1 : [3, 4],
    l
]
-----
[1, 3, 4, 2]
=====
remove
-----
_ do _[
    _remove set _ import .list.remove.context.let,
    _l set [1, 2, 3],
    [_l remove 1, l]
]
-----
[2, [1, 3]]
=====
remove_many
-----
_ do _[
    _remove_many set _ import .list.remove_many.context.let,
    _l set [1, 2, 3],
    [_l remove_many 1 : 3, l]
]
-----
[[2, 3], [1]]
=====
remove_many .
-----
_ do _[
    _remove_many set _ import .list.remove_many.context.let,
    _l set [1, 2, 3],
    _l remove_many . : .,
    l
]
-----
[]
=====
push
-----
_ do _[
    _push set _ import .list.push.context.let,
    _l set [1],
    _l push 2,
    l
]
-----
[1, 2]
=====
push_many
-----
_ do _[
    _push_many set _ import .list.push_many.context.let,
    _l set [1],
    _l push_many [2, 3],
    l
]
-----
[1, 2, 3]
=====
push_many empty
-----
_ do _[
    _push_many set _ import .list.push_many.context.let,
    _l set [1],
    _l push_many [],
    l
]
-----
[1]
=====
pop empty
-----
_ do _[
    _pop set _ import .list.pop.context.let,
    _config.make set _ import .config.make,
    _link.make set _ import .link.make,
    _config.let set _ import .config.let,
    _is_aborted set _ import .error.is_aborted.context.let,
    _c set _ config.make {
        .prelude : _ link.make {
            do : do,
            set : set,
            pop : pop,
        },
    },
    _c config.let _(_ do _[
        _l set [],
        _l pop .,
    ]),
    _c is_aborted .,
]
-----
true
=====
pop 1
-----
_ do _[
    _pop set _ import .list.pop.context.let,
    _l set [1],
    [_l pop ., l]
]
-----
[1, []]
=====
pop 2
-----
_ do _[
    _pop set _ import .list.pop.context.let,
    _l set [1, 2],
    [_l pop ., l]
]
-----
[2, [1]]
=====
pop_many 0
-----
_ do _[
    _pop_many set _ import .list.pop_many.context.let,
    _l set [1, 2],
    [_l pop_many 0, l]
]
-----
[[], [1, 2]]
=====
pop_many 1
-----
_ do _[
    _pop_many set _ import .list.pop_many.context.let,
    _l set [1, 2],
    [_l pop_many 1, l]
]
-----
[[2], [1]]
=====
pop_many else
-----
_ do _[
    _pop_many set _ import .list.pop_many.context.let,
    _l set [1, 2],
    [_l pop_many 2, l]
]
-----
[[1, 2], []]
=====
pop_many out of bound
-----
_ do _[
    _pop_many set _ import .list.pop_many.context.let,
    _config.make set _ import .config.make,
    _link.make set _ import .link.make,
    _config.let set _ import .config.let,
    _is_aborted set _ import .error.is_aborted.context.let,
    _c set _ config.make {
        .prelude : _ link.make {
            do : do,
            set : set,
            pop_many : pop_many,
        },
    },
    _c config.let _(_ do _[
        _l set [1, 2],
        _l pop_many 3,
    ]),
    _c is_aborted .,
]
-----
true
=====
clear
-----
_ do _[
    _clear set _ import .list.clear.context.let,
    _l set [1, 2],
    _l clear .,
    l
]
-----
[]
