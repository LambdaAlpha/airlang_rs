abort
-----
_ do _[
    _link.make set _ import .link.make,
    _config.let set _ import .config.let,
    _cfg set {
        .prelude : _ link.make {
            do : do,
            abort : abort,
        },
    },
    _(a : aborted) is _cfg config.let _(_ do _[
        _ abort .,
        1
    ]),
    aborted and a = .
]
-----
true
=====
assert false
-----
_ do _[
    _link.make set _ import .link.make,
    _config.let set _ import .config.let,
    _cfg set {
        .prelude : _ link.make {
            assert : assert,
        },
    },
    _(key'' : aborted) is _cfg config.let _(_ assert false),
    aborted
]
-----
true
=====
assert true
-----
_ do _[
    _link.make set _ import .link.make,
    _config.let set _ import .config.let,
    _cfg set {
        .prelude : _ link.make {
            assert : assert,
        },
    },
    _(key'' : aborted) is _cfg config.let _(_ assert true),
    aborted
]
-----
false
