parse generate_pretty generate_key
-----
_ do _[
    _any set _ import .value.any,
    _parse set _ import .language.syntax.parse,
    _generate_pretty set _ import .language.syntax.generate_pretty,
    _generate_key set _ import .language.syntax.generate_key,
    _into_text set _ import .key.into_text,
    _a set _ any .syntax,
    _repr set _a which generate_pretty : .,
    .(_a1) represent _ parse repr,
    _k set _a which generate_key : .,
    .(_a2) represent _ parse _ into_text k,
    (a = a1) and (a = a2)
]
-----
true
=====
generate_key equal
-----
_ do _[
    _any set _ import .value.any,
    _generate_key set _ import .language.syntax.generate_key,
    _a set _ any .,
    _b set _ any .,
    _a_key set _a which generate_key : .,
    _b_key set _b which generate_key : .,
    (a = b) = (a_key = b_key)
]
-----
true
=====
parse
-----
_ do _[
    _parse set _ import .language.syntax.parse,
    _ parse "[1, 2]"
]
-----
.([1, 2])
=====
parse no eval
-----
_ do _[
    _parse set _ import .language.syntax.parse,
    _a set 1,
    _ parse "a"
]
-----
.(a)
=====
parse text
-----
_ do _[
    _parse set _ import .language.syntax.parse,
    _ parse ""'"1"'
]
-----
.("1")
=====
generate_pretty text
-----
_ do _[
    _generate_pretty set _ import .language.syntax.generate_pretty,
    _a set "1",
    _a which generate_pretty : .
]
-----
""'"1"'
=====
any .syntax syntax is_valid
-----
_ do _[
    _any set _ import .value.any,
    _is_valid set _ import .language.syntax.is_valid,
    _a set _ any .syntax,
    _a which is_valid : .
]
-----
true
=====
syntax is_valid true
-----
_ do _[
    _is_valid set _ import .language.syntax.is_valid,
    _a set [., 0, "a" : {b : _b}, _(_ f _(c))],
    _a which is_valid : .
]
-----
true
=====
syntax is_valid false
-----
_ do _[
    _is_valid set _ import .language.syntax.is_valid,
    _a set [., 0, "a" : {b : not}, _(_ f _(c))],
    _a which is_valid : .
]
-----
false
=====
version
-----
_ do _[
    _check_version set _ import .dev.language.check_version,
    _ check_version _ import .language.version
]
-----
true
