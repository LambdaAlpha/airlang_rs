solve
-----
a ? b
=====
solve ctx
-----
?(a b)
=====
solve solve
-----
a ? b ? c
=====
solve other
-----
a ? b : c
=====
other solve
-----
a : b ? c
=====
solve infix
-----
a ? b c d
=====
infix solve
-----
a b c ? d
=====
non-atomic first
-----
.[a b] ? c
=====
non-atomic second
-----
a ? .[b c]
=====
list item
-----
[a ? b]
=====
map key
-----
{(a ? b) : c}
=====
map value
-----
{a : b ? c}
