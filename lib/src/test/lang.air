parse generate
-----
_ do [
    _any set _ import _value.any,
    _parse set _ import _language.syntax.parse,
    _generate set _ import _language.syntax.generate,
    _get set _ import _list.get,
    _% set _ import _integer.remainder,
    _atom set [_unit, _bit, _symbol, _text, _integer, _number, _byte],
    _i set 3 + (_ any _integer) % 4,
    _a set atom which _ get i,
    (_ any _bit) loop [
        (_ any _bit) test [
            _i set 3 + (_ any _integer) % 4,
            _b set atom which _ get i,
            _i set (4 + (_ any _integer) % 4) % 4,
            _a set (_ any _bit) test [a : b] : [_ data. _ a b],
        ] : [
            (_ any _bit) test [
                _i set 3 + (_ any _integer) % 4,
                _b set atom which _ get i,
                _a set [a, b],
            ] : [
                _i set 3 + (_ any _integer) % 4,
                _b set atom which _ get i,
                _a set {a : b},
            ]
        ]
    ],
    _a1 set _ parse _ generate a,
    a == a1
]
-----
true
=====
parse
-----
_ do [
    _parse set _ import _language.syntax.parse,
    _ parse "[1, 2]"
]
-----
[1, 2]
=====
parse no eval
-----
_ do [
    _parse set _ import _language.syntax.parse,
    _a set 1,
    _ parse "a"
]
-----
a
=====
parse text
-----
_ do [
    _parse set _ import _language.syntax.parse,
    _ parse "\`1\`"
]
-----
"1"
=====
generate text
-----
_ do [
    _generate set _ import _language.syntax.generate,
    _ generate "1"
]
-----
"\`1\`"
