length push
-----
_ do [
    length = _ import .list.length,
    push = _ import .list.push,
    a = _ any list,
    l1 = a ; length .,
    a ; push .,
    l2 = a ; length .,
    l2 == l1 + 1
]
-----
true
=====
length push_many
-----
_ do [
    length = _ import .list.length,
    push_many = _ import .list.push_many,
    a = _ any list,
    la = a ; length .,
    b = _ any list,
    lb = b ; length .,
    a ; push_many b,
    lab = a ; length .,
    lab == la + lb
]
-----
true
=====
length pop_many
-----
_ do [
    length = _ import .list.length,
    pop_many = _ import .list.pop_many,
    % = _ import .integer.remainder,
    a = _ any list,
    l1 = a ; length .,
    (l1 == 0) .return true,
    i = (l1 + (_ any integer) % l1) % l1,
    a ; pop_many i,
    l2 = a ; length .,
    l2 == l1 - i
]
-----
true
=====
set get
-----
_ do [
    length = _ import .list.length,
    set = _ import .list.set,
    get = _ import .list.get,
    % = _ import .integer.remainder,
    a = _ any list,
    b = a,
    l = a ; length .,
    (l == 0) .return true,
    i = (l + (_ any integer) % l) % l,
    v1 = a ; get i,
    v2 = _ any .,
    v3 = a ; set i : v2,
    v4 = a ; get i,
    a ; set i : v1,
    (v1 == v3) and (v2 == v4) and (a == b)
]
-----
true
=====
set_many get_many
-----
_ do [
    length = _ import .list.length,
    get_many = _ import .list.get_many,
    set_many = _ import .list.set_many,
    % = _ import .integer.remainder,
    a = _ any list,
    la = a ; length .,
    (la == 0) .return true,
    b = _ any list,
    lb = b ; length .,
    (lb == 0) .return true,
    (la < lb) ? [
        t = a,
        a = b,
        b = t,
        t = la,
        la = lb,
        lb = t,
    ] : [],
    a1 = a,
    d = la - lb,
    i = (d == 0) ? [0] : [(d + (_ any integer) % d) % d],
    c1 = a ; get_many i : i + lb,
    c2 = a ; set_many i : b,
    b1 = a ; get_many i : i + lb,
    a ; set_many i : c1,
    (b == b1) and (c1 == c2) and (a == a1)
]
-----
true
=====
insert get length
-----
_ do [
    length = _ import .list.length,
    insert = _ import .list.insert,
    get = _ import .list.get,
    % = _ import .integer.remainder,
    a = _ any list,
    l = 1 + a ; length .,
    i = (l + (_ any integer) % l) % l,
    v = _ any .,
    a ; insert i : v,
    v1 = a ; get i,
    l1 = a ; length .,
    (v == v1) and (l == l1)
]
-----
true
=====
insert remove
-----
_ do [
    length = _ import .list.length,
    insert = _ import .list.insert,
    remove = _ import .list.remove,
    % = _ import .integer.remainder,
    a = _ any list,
    b = a,
    l = 1 + a ; length .,
    i = (l + (_ any integer) % l) % l,
    v1 = _ any .,
    a ; insert i : v1,
    v2 = a ; remove i,
    (v1 == v2) and a == b
]
-----
true
=====
insert_many get_many length
-----
_ do [
    length = _ import .list.length,
    insert_many = _ import .list.insert_many,
    get_many = _ import .list.get_many,
    % = _ import .integer.remainder,
    a = _ any list,
    l1 = 1 + a ; length .,
    i = (l1 + (_ any integer) % l1) % l1,
    v1 = _ any list,
    vl = v1 ; length .,
    a ; insert_many i : v1,
    v2 = a ; get_many i : i + vl,
    l2 = a ; length .,
    (v1 == v2) and l2 == l1 + vl - 1,
]
-----
true
=====
insert_many remove_many
-----
_ do [
    length = _ import .list.length,
    insert_many = _ import .list.insert_many,
    remove_many = _ import .list.remove_many,
    % = _ import .integer.remainder,
    a = _ any list,
    b = a,
    l = 1 + a ; length .,
    i = (l + (_ any integer) % l) % l,
    v1 = _ any list,
    v1l = v1 ; length .,
    a ; insert_many i : v1,
    v2 = a ; remove_many i : i + v1l,
    (v1 == v2) and a == b
]
-----
true
=====
push pop
-----
_ do [
    push = _ import .list.push,
    pop = _ import .list.pop,
    a1 = _ any list,
    a2 = a1,
    v1 = _ any .,
    a1 ; push v1,
    v2 = a1 ; pop .,
    (v1 == v2) and (a1 == a2)
]
-----
true
=====
push_many pop_many
-----
_ do [
    length = _ import .list.length,
    push_many = _ import .list.push_many,
    pop_many = _ import .list.pop_many,
    a1 = _ any list,
    a2 = a1,
    b1 = _ any list,
    lb = b1 ; length .,
    a1 ; push_many b1,
    b2 = a1 ; pop_many lb,
    (b1 == b2) and a1 == a2
]
-----
true
=====
clear
-----
_ do [
    clear = _ import .list.clear,
    a = _ any list,
    a ; clear .,
    a == []
]
-----
true
=====
length empty
-----
_ do [
    length = _ import .list.length,
    l = [],
    l ; length .
]
-----
0
=====
length else
-----
_ do [
    length = _ import .list.length,
    l = [1, 2],
    l ; length .
]
-----
2
=====
set 1
-----
_ do [
    set = _ import .list.set,
    a = [1],
    a ; set 0 : 2,
    a
]
-----
[2]
=====
set 2
-----
_ do [
    set = _ import .list.set,
    a = [1, 2],
    a ; set 1 : 3,
    a
]
-----
[1, 3]
=====
set length
-----
_ do [
    set = _ import .list.set,
    a = [],
    a ; set 1 : 1,
    a
]
-----
[]
=====
set_many empty
-----
_ do [
    set_many = _ import .list.set_many,
    a = [0, 1, 2],
    a ; set_many 0 : []
]
-----
[]
=====
set_many 1
-----
_ do [
    set_many = _ import .list.set_many,
    a = [0, 1, 2],
    a ; set_many 0 : [3, 4],
    a
]
-----
[3, 4, 2]
=====
set_many 2
-----
_ do [
    set_many = _ import .list.set_many,
    a = [0, 1, 2],
    a ; set_many 1 : [3, 4],
    a
]
-----
[0, 3, 4]
=====
set_many last
-----
_ do [
    set_many = _ import .list.set_many,
    a = [0, 1, 2],
    a ; set_many 2 : [],
    a
]
-----
[0, 1, 2]
=====
get out of bound
-----
_ do [
    get = _ import .list.get,
    l = [],
    l ; get 0
]
-----
.
=====
get 0
-----
_ do [
    get = _ import .list.get,
    l = [1],
    l ; get 0
]
-----
1
=====
get else
-----
_ do [
    get = _ import .list.get,
    l = [1, 2],
    l ; get 1
]
-----
2
=====
get_many 1
-----
_ do [
    get_many = _ import .list.get_many,
    l = [1, 2],
    l ; get_many 0 : 1
]
-----
[1]
=====
get_many 2
-----
_ do [
    get_many = _ import .list.get_many,
    l = [1, 2],
    l ; get_many 1 : 2
]
-----
[2]
=====
get_many . 1
-----
_ do [
    get_many = _ import .list.get_many,
    l = [1, 2],
    l ; get_many . : .
]
-----
[1, 2]
=====
get_many . 2
-----
_ do [
    get_many = _ import .list.get_many,
    l = [1, 2],
    l ; get_many 1 : .
]
-----
[2]
=====
get_many . 3
-----
_ do [
    get_many = _ import .list.get_many,
    l = [1, 2],
    l ; get_many . : 1
]
-----
[1]
=====
insert out of bound
-----
_ do [
    insert = _ import .list.insert,
    l = [],
    l ; insert 1 : 1,
    l
]
-----
[]
=====
insert
-----
_ do [
    insert = _ import .list.insert,
    l = [1, 2],
    l ; insert 1 : 3,
    l
]
-----
[1, 3, 2]
=====
insert length
-----
_ do [
    insert = _ import .list.insert,
    a = [],
    a ; insert 0 : 1,
    a
]
-----
[1]
=====
insert_many
-----
_ do [
    insert_many = _ import .list.insert_many,
    l = [1, 2],
    l ; insert_many 1 : [3, 4],
    l
]
-----
[1, 3, 4, 2]
=====
remove
-----
_ do [
    remove = _ import .list.remove,
    l = [1, 2, 3],
    [l ; remove 1, l]
]
-----
[2, [1, 3]]
=====
remove_many
-----
_ do [
    remove_many = _ import .list.remove_many,
    l = [1, 2, 3],
    [l ; remove_many 1 : 3, l]
]
-----
[[2, 3], [1]]
=====
remove_many .
-----
_ do [
    remove_many = _ import .list.remove_many,
    l = [1, 2, 3],
    l ; remove_many . : .,
    l
]
-----
[]
=====
push
-----
_ do [
    push = _ import .list.push,
    l = [1],
    l ; push 2,
    l
]
-----
[1, 2]
=====
push_many
-----
_ do [
    push_many = _ import .list.push_many,
    l = [1],
    l ; push_many [2, 3],
    l
]
-----
[1, 2, 3]
=====
push_many empty
-----
_ do [
    push_many = _ import .list.push_many,
    l = [1],
    l ; push_many [],
    l
]
-----
[1]
=====
pop empty
-----
_ do [
    pop = _ import .list.pop,
    l = [],
    [l ; pop ., l]
]
-----
[., []]
=====
pop 1
-----
_ do [
    pop = _ import .list.pop,
    l = [1],
    [l ; pop ., l]
]
-----
[1, []]
=====
pop 2
-----
_ do [
    pop = _ import .list.pop,
    l = [1, 2],
    [l ; pop ., l]
]
-----
[2, [1]]
=====
pop_many 0
-----
_ do [
    pop_many = _ import .list.pop_many,
    l = [1, 2],
    [l ; pop_many 0, l]
]
-----
[[], [1, 2]]
=====
pop_many 1
-----
_ do [
    pop_many = _ import .list.pop_many,
    l = [1, 2],
    [l ; pop_many 1, l]
]
-----
[[2], [1]]
=====
pop_many else
-----
_ do [
    pop_many = _ import .list.pop_many,
    l = [1, 2],
    [l ; pop_many 2, l]
]
-----
[[1, 2], []]
=====
pop_many out of bound
-----
_ do [
    pop_many = _ import .list.pop_many,
    l = [1, 2],
    l ; pop_many 3,
    l
]
-----
[1, 2]
=====
clear
-----
_ do [
    clear = _ import .list.clear,
    l = [1, 2],
    l ; clear .,
    l
]
-----
[]
