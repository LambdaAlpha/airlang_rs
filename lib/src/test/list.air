length push
-----
_ do [
    any = _ import _value.any,
    length = _ import _list.length,
    push = _ import _list.push,
    a = _ any list,
    l1 = a which _ length .,
    a which _ push .,
    l2 = a which _ length .,
    l2 == l1 + 1
]
-----
true
=====
length push_many
-----
_ do [
    any = _ import _value.any,
    length = _ import _list.length,
    push_many = _ import _list.push_many,
    a = _ any list,
    la = a which _ length .,
    b = _ any list,
    lb = b which _ length .,
    a which _ push_many b,
    lab = a which _ length .,
    lab == la + lb
]
-----
true
=====
length pop_many
-----
_ do [
    any = _ import _value.any,
    length = _ import _list.length,
    pop_many = _ import _list.pop_many,
    % = _ import _integer.remainder,
    a = _ any list,
    l1 = a which _ length .,
    (l1 == 0) _return true,
    i = (l1 + (_ any integer) % l1) % l1,
    a which _ pop_many i,
    l2 = a which _ length .,
    l2 == l1 - i
]
-----
true
=====
set get
-----
_ do [
    any = _ import _value.any,
    length = _ import _list.length,
    set = _ import _list.set,
    get = _ import _list.get,
    % = _ import _integer.remainder,
    a = _ any list,
    b = a,
    l = a which _ length .,
    (l == 0) _return true,
    i = (l + (_ any integer) % l) % l,
    v1 = a which _ get i,
    v2 = _ any .,
    v3 = a which _ set i : v2,
    v4 = a which _ get i,
    a which _ set i : v1,
    (v1 == v3) and (v2 == v4) and (a == b)
]
-----
true
=====
set_many get_many
-----
_ do [
    any = _ import _value.any,
    length = _ import _list.length,
    get_many = _ import _list.get_many,
    set_many = _ import _list.set_many,
    % = _ import _integer.remainder,
    a = _ any list,
    la = a which _ length .,
    (la == 0) _return true,
    b = _ any list,
    lb = b which _ length .,
    (lb == 0) _return true,
    (la < lb) test [
        t = a,
        a = b,
        b = t,
        t = la,
        la = lb,
        lb = t,
    ] : [],
    a1 = a,
    d = la - lb,
    i = (d == 0) test [0] : [(d + (_ any integer) % d) % d],
    c1 = a which _ get_many i : i + lb,
    c2 = a which _ set_many i : b,
    b1 = a which _ get_many i : i + lb,
    a which _ set_many i : c1,
    (b == b1) and (c1 == c2) and (a == a1)
]
-----
true
=====
insert get length
-----
_ do [
    any = _ import _value.any,
    length = _ import _list.length,
    insert = _ import _list.insert,
    get = _ import _list.get,
    % = _ import _integer.remainder,
    a = _ any list,
    l = 1 + a which _ length .,
    i = (l + (_ any integer) % l) % l,
    v = _ any .,
    a which _ insert i : v,
    v1 = a which _ get i,
    l1 = a which _ length .,
    (v == v1) and (l == l1)
]
-----
true
=====
insert remove
-----
_ do [
    any = _ import _value.any,
    length = _ import _list.length,
    insert = _ import _list.insert,
    remove = _ import _list.remove,
    % = _ import _integer.remainder,
    a = _ any list,
    b = a,
    l = 1 + a which _ length .,
    i = (l + (_ any integer) % l) % l,
    v1 = _ any .,
    a which _ insert i : v1,
    v2 = a which _ remove i,
    (v1 == v2) and a == b
]
-----
true
=====
insert_many get_many length
-----
_ do [
    any = _ import _value.any,
    length = _ import _list.length,
    insert_many = _ import _list.insert_many,
    get_many = _ import _list.get_many,
    % = _ import _integer.remainder,
    a = _ any list,
    l1 = 1 + a which _ length .,
    i = (l1 + (_ any integer) % l1) % l1,
    v1 = _ any list,
    vl = v1 which _ length .,
    a which _ insert_many i : v1,
    v2 = a which _ get_many i : i + vl,
    l2 = a which _ length .,
    (v1 == v2) and l2 == l1 + vl - 1,
]
-----
true
=====
insert_many remove_many
-----
_ do [
    any = _ import _value.any,
    length = _ import _list.length,
    insert_many = _ import _list.insert_many,
    remove_many = _ import _list.remove_many,
    % = _ import _integer.remainder,
    a = _ any list,
    b = a,
    l = 1 + a which _ length .,
    i = (l + (_ any integer) % l) % l,
    v1 = _ any list,
    v1l = v1 which _ length .,
    a which _ insert_many i : v1,
    v2 = a which _ remove_many i : i + v1l,
    (v1 == v2) and a == b
]
-----
true
=====
push pop
-----
_ do [
    any = _ import _value.any,
    push = _ import _list.push,
    pop = _ import _list.pop,
    a1 = _ any list,
    a2 = a1,
    v1 = _ any .,
    a1 which _ push v1,
    v2 = a1 which _ pop .,
    (v1 == v2) and (a1 == a2)
]
-----
true
=====
push_many pop_many
-----
_ do [
    any = _ import _value.any,
    length = _ import _list.length,
    push_many = _ import _list.push_many,
    pop_many = _ import _list.pop_many,
    a1 = _ any list,
    a2 = a1,
    b1 = _ any list,
    lb = b1 which _ length .,
    a1 which _ push_many b1,
    b2 = a1 which _ pop_many lb,
    (b1 == b2) and a1 == a2
]
-----
true
=====
clear
-----
_ do [
    any = _ import _value.any,
    clear = _ import _list.clear,
    a = _ any list,
    a which _ clear .,
    a == []
]
-----
true
=====
length empty
-----
_ do [
    length = _ import _list.length,
    l = [],
    l which _ length .
]
-----
0
=====
length else
-----
_ do [
    length = _ import _list.length,
    l = [1, 2],
    l which _ length .
]
-----
2
=====
set 1
-----
_ do [
    set = _ import _list.set,
    a = [1],
    a which _ set 0 : 2,
    a
]
-----
[2]
=====
set 2
-----
_ do [
    set = _ import _list.set,
    a = [1, 2],
    a which _ set 1 : 3,
    a
]
-----
[1, 3]
=====
set length
-----
_ do [
    set = _ import _list.set,
    a = [],
    a which _ set 1 : 1,
    a
]
-----
[]
=====
set_many empty
-----
_ do [
    set_many = _ import _list.set_many,
    a = [0, 1, 2],
    a which _ set_many 0 : []
]
-----
[]
=====
set_many 1
-----
_ do [
    set_many = _ import _list.set_many,
    a = [0, 1, 2],
    a which _ set_many 0 : [3, 4],
    a
]
-----
[3, 4, 2]
=====
set_many 2
-----
_ do [
    set_many = _ import _list.set_many,
    a = [0, 1, 2],
    a which _ set_many 1 : [3, 4],
    a
]
-----
[0, 3, 4]
=====
set_many last
-----
_ do [
    set_many = _ import _list.set_many,
    a = [0, 1, 2],
    a which _ set_many 2 : [],
    a
]
-----
[0, 1, 2]
=====
get out of bound
-----
_ do [
    get = _ import _list.get,
    l = [],
    l which _ get 0
]
-----
.
=====
get 0
-----
_ do [
    get = _ import _list.get,
    l = [1],
    l which _ get 0
]
-----
1
=====
get else
-----
_ do [
    get = _ import _list.get,
    l = [1, 2],
    l which _ get 1
]
-----
2
=====
get_many 1
-----
_ do [
    get_many = _ import _list.get_many,
    l = [1, 2],
    l which _ get_many 0 : 1
]
-----
[1]
=====
get_many 2
-----
_ do [
    get_many = _ import _list.get_many,
    l = [1, 2],
    l which _ get_many 1 : 2
]
-----
[2]
=====
get_many . 1
-----
_ do [
    get_many = _ import _list.get_many,
    l = [1, 2],
    l which _ get_many . : .
]
-----
[1, 2]
=====
get_many . 2
-----
_ do [
    get_many = _ import _list.get_many,
    l = [1, 2],
    l which _ get_many 1 : .
]
-----
[2]
=====
get_many . 3
-----
_ do [
    get_many = _ import _list.get_many,
    l = [1, 2],
    l which _ get_many . : 1
]
-----
[1]
=====
insert out of bound
-----
_ do [
    insert = _ import _list.insert,
    l = [],
    l which _ insert 1 : 1,
    l
]
-----
[]
=====
insert
-----
_ do [
    insert = _ import _list.insert,
    l = [1, 2],
    l which _ insert 1 : 3,
    l
]
-----
[1, 3, 2]
=====
insert length
-----
_ do [
    insert = _ import _list.insert,
    a = [],
    a which _ insert 0 : 1,
    a
]
-----
[1]
=====
insert_many
-----
_ do [
    insert_many = _ import _list.insert_many,
    l = [1, 2],
    l which _ insert_many 1 : [3, 4],
    l
]
-----
[1, 3, 4, 2]
=====
remove
-----
_ do [
    remove = _ import _list.remove,
    l = [1, 2, 3],
    [l which _ remove 1, l]
]
-----
[2, [1, 3]]
=====
remove_many
-----
_ do [
    remove_many = _ import _list.remove_many,
    l = [1, 2, 3],
    [l which _ remove_many 1 : 3, l]
]
-----
[[2, 3], [1]]
=====
remove_many .
-----
_ do [
    remove_many = _ import _list.remove_many,
    l = [1, 2, 3],
    l which _ remove_many . : .,
    l
]
-----
[]
=====
push
-----
_ do [
    push = _ import _list.push,
    l = [1],
    l which _ push 2,
    l
]
-----
[1, 2]
=====
push_many
-----
_ do [
    push_many = _ import _list.push_many,
    l = [1],
    l which _ push_many [2, 3],
    l
]
-----
[1, 2, 3]
=====
push_many empty
-----
_ do [
    push_many = _ import _list.push_many,
    l = [1],
    l which _ push_many [],
    l
]
-----
[1]
=====
pop empty
-----
_ do [
    pop = _ import _list.pop,
    l = [],
    [l which _ pop ., l]
]
-----
[., []]
=====
pop 1
-----
_ do [
    pop = _ import _list.pop,
    l = [1],
    [l which _ pop ., l]
]
-----
[1, []]
=====
pop 2
-----
_ do [
    pop = _ import _list.pop,
    l = [1, 2],
    [l which _ pop ., l]
]
-----
[2, [1]]
=====
pop_many 0
-----
_ do [
    pop_many = _ import _list.pop_many,
    l = [1, 2],
    [l which _ pop_many 0, l]
]
-----
[[], [1, 2]]
=====
pop_many 1
-----
_ do [
    pop_many = _ import _list.pop_many,
    l = [1, 2],
    [l which _ pop_many 1, l]
]
-----
[[2], [1]]
=====
pop_many else
-----
_ do [
    pop_many = _ import _list.pop_many,
    l = [1, 2],
    [l which _ pop_many 2, l]
]
-----
[[1, 2], []]
=====
pop_many out of bound
-----
_ do [
    pop_many = _ import _list.pop_many,
    l = [1, 2],
    l which _ pop_many 3,
    l
]
-----
[1, 2]
=====
clear
-----
_ do [
    clear = _ import _list.clear,
    l = [1, 2],
    l which _ clear .,
    l
]
-----
[]
