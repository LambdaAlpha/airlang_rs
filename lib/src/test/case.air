
-----
do [
    c = case ; {
        function : not,
        input : 1,
        output : 2,
    },
    c case.input .
]
-----
1
=====

-----
do [
    c = case ; {
        function : not,
        input : 1,
        output : 2,
    },
    c case.output .
]
-----
2
=====

-----
do [
    c = case ; {
        function : not,
        input : 1,
        output : 2,
    },
    (c case.function .) == not
]
-----
true
=====

-----
do [
    c = case ; {
        function : "not",
        input : 1,
        output : 2,
    },
    (c case.function .) == "not"
]
-----
true
=====

-----
do [
    map_mode = mode ; {map : {} : id : eval*},
    m = map_mode ; {
        function : not,
        input : true,
        output : false,
    },
    m == case.represent ; case ; m
]
-----
true
=====

-----
do [
    m = case.represent ; case.cache ; not ; true,
    map_mode = mode ; {map : {} : id : eval*},
    expected = map_mode ; {
        function : not,
        input : true,
        output : false,
        is_cache : true,
    },
    m == expected
]
-----
true
=====

-----
do [
    c = case ; {
        function : not,
        input : 1,
        output : 2,
    },
    tc = c type .,
    (tc == .case) and not ; c case.is_cache .
]
-----
true
=====

-----
do [
    c = case.cache ; not ; true,
    c case.input .
]
-----
true
=====

-----
do [
    c = case.cache ; not ; true,
    c case.output .
]
-----
false
=====

-----
do [
    c = case.cache ; not ; true,
    (c case.function .) == not
]
-----
true
=====

-----
do [
    c = case.cache ; not ; true,
    c case.is_cache .
]
-----
true
