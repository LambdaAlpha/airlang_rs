length join
-----
_ do [
    _any set _ import _value.any,
    _length set _ import _symbol.length,
    _join set _ import _symbol.join,
    _a set _ any _symbol,
    _la set a which _ length .,
    _b set _ any _symbol,
    _lb set b which _ length .,
    _ab set . join [a, b],
    _lab set ab which _ length .,
    lab == la + lb
]
-----
true
=====
length empty
-----
_ do [
    _length set _ import _symbol.length,
    empty which _ length .
]
-----
0
=====
length else
-----
_ do [
    _length set _ import _symbol.length,
    _s set _a,
    s which _ length .
]
-----
1
=====
join empty
-----
_ do [
    _join set _ import _symbol.join,
    empty join [_a, _b]
]
-----
ab
=====
join unit
-----
_ do [
    _join set _ import _symbol.join,
    _a set _a1,
    _b set _b1,
    _c set _c1,
    . join [a, b, c]
]
-----
a1b1c1
=====
join no symbol
-----
_ do [
    _join set _ import _symbol.join,
    . join []
]
-----
''
=====
join else
-----
_ do [
    _join set _ import _symbol.join,
    _. join [_a, _b]
]
-----
a.b
=====
into_text from_text
-----
_ do [
    _any set _ import _value.any,
    _from_text set _ import _symbol.from_text,
    _into_text set _ import _symbol.into_text,
    _s set _ any _symbol,
    _s1 set _ from_text _ into_text s,
    s == s1
]
-----
true
=====
from_text empty
-----
_ do [
    _from_text set _ import _symbol.from_text,
    _ from_text ""
]
-----
''
=====
from_text trivial
-----
_ do [
    _from_text set _ import _symbol.from_text,
    _ from_text "hello_world!"
]
-----
hello_world!
=====
from_text special
-----
_ do [
    _from_text set _ import _symbol.from_text,
    _ from_text "{}"
]
-----
'{}'
=====
into_text empty
-----
_ do [
    _into_text set _ import _symbol.into_text,
    _ into_text empty
]
-----
""
=====
into_text trivial
-----
_ do [
    _into_text set _ import _symbol.into_text,
    _ into_text _hello_world!
]
-----
"hello_world!"
