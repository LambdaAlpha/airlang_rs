length join
-----
_ do [
    any = _ import _value.any,
    length = _ import _symbol.length,
    join = _ import _symbol.join,
    a = _ any symbol,
    la = a which _ length .,
    b = _ any symbol,
    lb = b which _ length .,
    ab = . join [a, b],
    lab = ab which _ length .,
    lab == la + lb
]
-----
true
=====
length empty
-----
_ do [
    length = _ import _symbol.length,
    s = _ form '',
    s which _ length .
]
-----
0
=====
length else
-----
_ do [
    length = _ import _symbol.length,
    s = _a,
    s which _ length .
]
-----
1
=====
join empty
-----
_ do [
    join = _ import _symbol.join,
    '.' join [_a, _b]
]
-----
ab
=====
join unit
-----
_ do [
    join = _ import _symbol.join,
    a = _a1,
    b = _b1,
    c = _c1,
    . join [a, b, c]
]
-----
a1b1c1
=====
join no symbol
-----
_ do [
    join = _ import _symbol.join,
    . join []
]
-----
''
=====
join else
-----
_ do [
    join = _ import _symbol.join,
    _. join [_a, _b]
]
-----
a.b
=====
into_text from_text
-----
_ do [
    any = _ import _value.any,
    from_text = _ import _symbol.from_text,
    into_text = _ import _symbol.into_text,
    s = _ any symbol,
    s1 = _ from_text _ into_text s,
    s == s1
]
-----
true
=====
from_text empty
-----
_ do [
    from_text = _ import _symbol.from_text,
    _ from_text ""
]
-----
''
=====
from_text trivial
-----
_ do [
    from_text = _ import _symbol.from_text,
    _ from_text "hello_world!"
]
-----
hello_world!
=====
from_text special
-----
_ do [
    from_text = _ import _symbol.from_text,
    _ from_text "{}"
]
-----
'{}'
=====
into_text empty
-----
_ do [
    into_text = _ import _symbol.into_text,
    _ into_text _ form ''
]
-----
""
=====
into_text trivial
-----
_ do [
    into_text = _ import _symbol.into_text,
    _ into_text _hello_world!
]
-----
"hello_world!"
