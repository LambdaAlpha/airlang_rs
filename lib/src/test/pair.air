get
-----
_ do [
    .any set _ import _value.any,
    .first set _ import _pair.first,
    .second set _ import _pair.second,
    .f set _ any .,
    .s set _ any .,
    .p set f : s,
    (f == p which _ first .) and (s == p which _ second .)
]
-----
true
=====
set get
-----
_ do [
    .any set _ import _value.any,
    .first set _ import _pair.first,
    .second set _ import _pair.second,
    .set_first set _ import _pair.set_first,
    .set_second set _ import _pair.set_second,
    .p set _ any _pair,
    .f set _ any .,
    .s set _ any .,
    p which _ set_first f,
    p which _ set_second s,
    .f1 set p which _ first .,
    .s1 set p which _ second .,
    (f == f1) and (s == s1)
]
-----
true
=====
set return
-----
_ do [
    .any set _ import _value.any,
    .set_first set _ import _pair.set_first,
    .set_second set _ import _pair.set_second,
    .f1 set _ any .,
    .s1 set _ any .,
    .p set f1 : s1,
    .f2 set p which _ set_first _ any .,
    .s2 set p which _ set_second _ any .,
    (f1 == f2) and s1 == s2
]
-----
true
