get
-----
_ do [
    any = _ import .value.any,
    first = _ import .pair.first,
    second = _ import .pair.second,
    f = _ any .,
    s = _ any .,
    p = f : s,
    (f == p which _ first .) and (s == p which _ second .)
]
-----
true
=====
set get
-----
_ do [
    any = _ import .value.any,
    first = _ import .pair.first,
    second = _ import .pair.second,
    set_first = _ import .pair.set_first,
    set_second = _ import .pair.set_second,
    p = _ any pair,
    f = _ any .,
    s = _ any .,
    p which _ set_first f,
    p which _ set_second s,
    f1 = p which _ first .,
    s1 = p which _ second .,
    (f == f1) and (s == s1)
]
-----
true
=====
set return
-----
_ do [
    any = _ import .value.any,
    set_first = _ import .pair.set_first,
    set_second = _ import .pair.set_second,
    f1 = _ any .,
    s1 = _ any .,
    p = f1 : s1,
    f2 = p which _ set_first _ any .,
    s2 = p which _ set_second _ any .,
    (f1 == f2) and s1 == s2
]
-----
true
