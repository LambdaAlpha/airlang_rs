new which self
-----
_ do [
    .any set _ import _value.any,
    .new set _ import _link.new,
    .which set _ import _link.which,
    .self set _ import _context.self,
    .a set _ any .,
    .l set _ new a,
    .a1 set l which self : .,
    a == a1
]
-----
true
=====
new_constant which self
-----
_ do [
    .any set _ import _value.any,
    .new set _ import _link.new_constant,
    .which set _ import _link.which,
    .self set _ import _context.self,
    .a set _ any .,
    .l set _ new a,
    .a1 set l which self : .,
    a == a1
]
-----
true
=====
const is_constant
-----
_ do [
    .new_constant set _ import _link.new_constant,
    .is_constant set _ import _link.is_constant,
    .l set _ new_constant {},
    _ is_constant l,
]
-----
true
=====
mut is_constant
-----
_ do [
    .new set _ import _link.new,
    .is_constant set _ import _link.is_constant,
    .l set _ new {},
    _ is_constant l,
]
-----
false
=====
sync
-----
_ do [
    .any set _ import _value.any,
    .new set _ import _link.new,
    .which set _ import _link.which,
    .l set _ new {},
    .l1 set l,
    .a set _ any .,
    l which set : _a : a,
    .a1 set l1 which get : _a,
    a == a1
]
-----
true
=====
leak no panic
-----
_ do [
    .any set _ import _value.any,
    .new set _ import _link.new,
    .which set _ import _link.which,
    .l set _ new {},
    l which set : _a : l,
    .l move _,
    .
]
-----
.
=====
cycle recover
-----
_ do [
    .any set _ import _value.any,
    .new set _ import _link.new,
    .which set _ import _link.which,
    .l set _ new {},
    l which set : _a : l,
    .l1 set l which move : _a,
    l == l1
]
-----
true
