parse generate
-----
_ do [
    any = _ import _value.any,
    parse = _ import _syntax.parse,
    generate = _ import _syntax.generate,
    get = _ import _list.get,
    % = _ import _integer.remainder,
    atom = [_unit, _bit, _symbol, _text, _integer, _number, _byte],
    i = 3 + (_ any _integer) % 4,
    a = atom which _ get i,
    (_ any _bit) loop [
        (_ any _bit) test [
            i = 3 + (_ any _integer) % 4,
            b = atom which _ get i,
            i = (4 + (_ any _integer) % 4) % 4,
            a = (_ any _bit) test [a : b] : [_ data. _ a b],
        ] : [
            (_ any _bit) test [
                i = 3 + (_ any _integer) % 4,
                b = atom which _ get i,
                a = [a, b],
            ] : [
                i = 3 + (_ any _integer) % 4,
                b = atom which _ get i,
                a = {a : b},
            ]
        ]
    ],
    a1 = _ parse _ generate a,
    a == a1
]
-----
true
=====
parse
-----
_ do [
    parse = _ import _syntax.parse,
    _ parse "[1, 2]"
]
-----
[1, 2]
=====
parse no eval
-----
_ do [
    parse = _ import _syntax.parse,
    a = 1,
    _ parse "a"
]
-----
a
=====
parse text
-----
_ do [
    parse = _ import _syntax.parse,
    _ parse "\`1\`"
]
-----
"1"
=====
generate text
-----
_ do [
    generate = _ import _syntax.generate,
    _ generate "1"
]
-----
"\`1\`"
