get_length join
-----
_ do _[
    _any set _ import .value.any,
    _get_length set _ import .key.get_length,
    _join set _ import .key.join,
    _a set _ any .key,
    _la set _a which get_length : .,
    _b set _ any .key,
    _lb set _b which get_length : .,
    _ab set empty join [a, b],
    _lab set _ab which get_length : .,
    lab = la + lb
]
-----
true
=====
get_length empty
-----
_ do _[
    _get_length set _ import .key.get_length,
    _empty which get_length : .
]
-----
0
=====
get_length else
-----
_ do _[
    _get_length set _ import .key.get_length,
    _s set .a,
    _s which get_length : .
]
-----
2
=====
join empty
-----
_ do _[
    _join set _ import .key.join,
    empty join [_a, _b]
]
-----
ab
=====
join no key
-----
_ do _[
    _join set _ import .key.join,
    empty join []
]
-----
''
=====
join else
-----
_ do _[
    _join set _ import .key.join,
    _. join [_a, _b]
]
-----
a.b
=====
into_text from_text
-----
_ do _[
    _any set _ import .value.any,
    _from_text set _ import .key.from_text,
    _into_text set _ import .key.into_text,
    _s set _ any .key,
    _s1 set _ from_text _ into_text s,
    s = s1
]
-----
true
=====
from_text empty
-----
_ do _[
    _from_text set _ import .key.from_text,
    _ from_text ""
]
-----
''
=====
from_text trivial
-----
_ do _[
    _from_text set _ import .key.from_text,
    _ from_text "hello_world!"
]
-----
hello_world!
=====
from_text special
-----
_ do _[
    _from_text set _ import .key.from_text,
    _ from_text "{}"
]
-----
'{}'
=====
into_text empty
-----
_ do _[
    _into_text set _ import .key.into_text,
    _ into_text empty
]
-----
""
=====
into_text trivial
-----
_ do _[
    _into_text set _ import .key.into_text,
    _ into_text _hello_world!
]
-----
"hello_world!"
