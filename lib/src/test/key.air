length join
-----
_ do [
    .any set _ import _value.any,
    .length set _ import _key.length,
    .join set _ import _key.join,
    .a set _ any _key,
    .la set a which _ length .,
    .b set _ any _key,
    .lb set b which _ length .,
    .ab set . join [a, b],
    .lab set ab which _ length .,
    lab == la + lb
]
-----
true
=====
length empty
-----
_ do [
    .length set _ import _key.length,
    empty which _ length .
]
-----
0
=====
length else
-----
_ do [
    .length set _ import _key.length,
    .s set _a,
    s which _ length .
]
-----
2
=====
join empty
-----
_ do [
    .join set _ import _key.join,
    empty join [.a, .b]
]
-----
ab
=====
join unit
-----
_ do [
    .join set _ import _key.join,
    .a set .a1,
    .b set .b1,
    .c set .c1,
    . join [a, b, c]
]
-----
a1b1c1
=====
join no key
-----
_ do [
    .join set _ import _key.join,
    . join []
]
-----
''
=====
join else
-----
_ do [
    .join set _ import _key.join,
    .. join [.a, .b]
]
-----
a.b
=====
into_text from_text
-----
_ do [
    .any set _ import _value.any,
    .from_text set _ import _key.from_text,
    .into_text set _ import _key.into_text,
    .s set _ any _key,
    .s1 set _ from_text _ into_text s,
    s == s1
]
-----
true
=====
from_text empty
-----
_ do [
    .from_text set _ import _key.from_text,
    _ from_text ""
]
-----
''
=====
from_text trivial
-----
_ do [
    .from_text set _ import _key.from_text,
    _ from_text "hello_world!"
]
-----
hello_world!
=====
from_text special
-----
_ do [
    .from_text set _ import _key.from_text,
    _ from_text "{}"
]
-----
'{}'
=====
into_text empty
-----
_ do [
    .into_text set _ import _key.into_text,
    _ into_text empty
]
-----
""
=====
into_text trivial
-----
_ do [
    .into_text set _ import _key.into_text,
    _ into_text .hello_world!
]
-----
"hello_world!"
