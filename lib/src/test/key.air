get_length join
-----
_ do _[
    .any set _ import _value.any,
    .get_length set _ import _key.get_length,
    .join set _ import _key.join,
    .a set _ any _key,
    .la set .a which get_length : .,
    .b set _ any _key,
    .lb set .b which get_length : .,
    .ab set empty join [a, b],
    .lab set .ab which get_length : .,
    lab == la + lb
]
-----
true
=====
get_length empty
-----
_ do _[
    .get_length set _ import _key.get_length,
    .empty which get_length : .
]
-----
0
=====
get_length else
-----
_ do _[
    .get_length set _ import _key.get_length,
    .s set _a,
    .s which get_length : .
]
-----
2
=====
join empty
-----
_ do _[
    .join set _ import _key.join,
    empty join [.a, .b]
]
-----
ab
=====
join no key
-----
_ do _[
    .join set _ import _key.join,
    empty join []
]
-----
''
=====
join else
-----
_ do _[
    .join set _ import _key.join,
    .. join [.a, .b]
]
-----
a.b
=====
into_text from_text
-----
_ do _[
    .any set _ import _value.any,
    .from_text set _ import _key.from_text,
    .into_text set _ import _key.into_text,
    .s set _ any _key,
    .s1 set _ from_text _ into_text s,
    s == s1
]
-----
true
=====
from_text empty
-----
_ do _[
    .from_text set _ import _key.from_text,
    _ from_text ""
]
-----
''
=====
from_text trivial
-----
_ do _[
    .from_text set _ import _key.from_text,
    _ from_text "hello_world!"
]
-----
hello_world!
=====
from_text special
-----
_ do _[
    .from_text set _ import _key.from_text,
    _ from_text "{}"
]
-----
'{}'
=====
into_text empty
-----
_ do _[
    .into_text set _ import _key.into_text,
    _ into_text empty
]
-----
""
=====
into_text trivial
-----
_ do _[
    .into_text set _ import _key.into_text,
    _ into_text .hello_world!
]
-----
"hello_world!"
