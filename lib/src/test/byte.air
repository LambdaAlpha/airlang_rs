length push
-----
_ do [
    any = _ import _value.any,
    length = _ import _byte.length,
    push = _ import _byte.push,
    b = _ any byte,
    l = b which _ length .,
    b which _ push byte(00),
    l1 = b which _ length .,
    l1 == l + 1
]
-----
true
=====
length join
-----
_ do [
    any = _ import _value.any,
    length = _ import _byte.length,
    join = _ import _byte.join,
    a = _ any byte,
    la = a which _ length .,
    b = _ any byte,
    lb = b which _ length .,
    ab = . join [a, b],
    lab = ab which _ length .,
    lab == la + lb
]
-----
true
=====
length empty
-----
_ do [
    length = _ import _byte.length,
    b = byte(),
    b which _ length .
]
-----
0
=====
length else
-----
_ do [
    length = _ import _byte.length,
    b = byte(ff),
    b which _ length .
]
-----
1
=====
push join
-----
_ do [
    any = _ import _value.any,
    push = _ import _byte.push,
    join = _ import _byte.join,
    a = _ any byte,
    b = _ any byte,
    ab = a,
    ab which _ push b,
    ab1 = . join [a, b],
    ab == ab1
]
-----
true
=====
push
-----
_ do [
    push = _ import _byte.push,
    a = byte(00),
    b = byte(1122),
    a which _ push b,
    a
]
-----
byte(001122)
=====
join empty
-----
_ do [
    join = _ import _byte.join,
    byte() join [byte(1234), byte(56)]
]
-----
byte(123456)
=====
join unit
-----
_ do [
    join = _ import _byte.join,
    a = byte(1234),
    b = byte(5678),
    c = byte(9abc),
    . join [a, b, c]
]
-----
byte(123456789abc)
=====
join no byte
-----
_ do [
    join = _ import _byte.join,
    . join []
]
-----
byte()
=====
join else
-----
_ do [
    join = _ import _byte.join,
    byte(00) join [byte(1111), byte(22)]
]
-----
byte(11110022)
