recursive call
-----
_ do _[
    _f set _ function {
        code : (. : _i) : _i,
        prelude : {},
    },
    _ f _ not true
]
-----
false
=====
call external func
-----
_ do _[
    _f set _ function {
        code : (. : _i) : not call _i,
        prelude : {},
    },
    _ f _ not true
]
-----
true
=====
call internal func
-----
_ do _[
    _f set _ function {
        code : _((. : i) : _ not i),
        prelude : {
            not : not
        },
    },
    _ f _ not true
]
-----
true
=====
call rename
-----
_ do _[
    _f set _ function {
        code : _((. : a) : _ n a),
        prelude : {
            n : not
        },
    },
    _ f _ not true
]
-----
true
=====
stateful
-----
_ do _[
    _link.make set _ import .link.make,
    _link.which set _ import .link.which,
    _f set _ function {
        code : (. : .) : _(_ do _[
            _i set a link.which get : .i,
            a link.which set : .i : i + 1,
            i
        ]),
        prelude : {
            a : _ link.make {.i : 0},
            do : do,
            set : set,
            link.which : link.which,
            get : get,
            + : +,
        },
    },
    [_ f ., _ f ., _ f .]
]
-----
[0, 1, 2]
=====
multiple stateful
-----
_ do _[
    _link.make set _ import .link.make,
    _link.which set _ import .link.which,
    _f set _(_ function {
        code : _((. : .) : _ do _[
            _i set a link.which get : .cell.value,
            a link.which set : .cell.value : i + 1,
            i
        ]),
        prelude : {
            a : _ link.make .(0),
            do : do,
            set : set,
            link.which : link.which,
            get : get,
            + : +,
        },
    }),
    _f1 set _ eval f,
    _f2 set _ eval f,
    [_ f1 ., _ f1 ., _ f1 ., _ f2 ., _ f2 ., _ f2 .]
]
-----
[0, 1, 2, 0, 1, 2]
=====
const const
-----
_ do _[
    _link.which set _ import .link.which,
    .a set 1,
    _f set _ function {
        code : _((c : .) : c link.which get : .a),
        constant : true,
        prelude : {
            link.which : link.which,
            get : get,
        },
    },
    _ f .
]
-----
1
=====
mutable const
-----
_ do _[
    _link.which set _ import .link.which,
    .a set 1,
    _f set _ function {
        code : _((c : .) : c link.which get : .a),
        constant : false,
        prelude : {
            link.which : link.which,
            get : get,
        },
    },
    _ f .
]
-----
1
=====
mutable mutate
-----
_ do _[
    _link.which set _ import .link.which,
    _f set _ function {
        code : _((c : i) : c link.which set : i : 2),
        constant : false,
        prelude : {
            link.which : link.which,
            set : set,
        },
    },
    _a set 1,
    _ f _a,
    a
]
-----
2
=====
const mutate fail
-----
_ do _[
    _link.which set _ import .link.which,
    _config.make set _ import .config.make,
    _link.make set _ import .link.make,
    _where set _ import .config.where,
    _is_aborted set _ import .error.is_aborted,
    _f set _ function {
        code : _((c : i) : c link.which set : i : 2),
        constant : true,
        prelude : {
            link.which : link.which,
            set : set,
        },
    },
    _c set _ config.make {
        .prelude : _ link.make {
            f : f,
            a : 1,
        },
    },
    _c where _(_ f _a),
    _c which is_aborted : .,
]
-----
true
=====
const free
-----
_ do _[
    _a set .,
    _f set _ function {
        code : (.c : .) : 1,
        constant : true,
        prelude : {},
    },
    _a which f : .
]
-----
1
=====
ctx name occupied
-----
_ do _[
    _a set 1,
    _f set _ function {
        code : (_ctx : .) : 2,
        constant : true,
        prelude : {
            ctx : 3
        },
    },
    _ f .,
    a = 1
]
-----
true
=====
ctx explicit
-----
_ do _[
    _link.which set _ import .link.which,
    _set_left set _ import .pair.set_left,
    _a set 1 : 2,
    _f set _ function {
        code : _((p : i) : p link.which set_left : i),
        prelude : {
            link.which : link.which,
            set_left : set_left,
        },
    },
    _a which f : 3,
    a
]
-----
3 : 2
=====
repr
-----
_ do _[
    _represent set _ import .function.represent,
    _ represent _ function {
        code : (_c : _i) : _i,
        prelude : {},
    }
]

-----
{
    constant : false,
    code : (c : i) : i,
    prelude : {}
}
=====
repr primitive free
-----
_ do _[
    _represent set _ import .function.represent,
    _function.get_code set _ import .function.get_code,
    _not_code set _not which function.get_code : .,
    _repr set {
        constant : true,
        code : not_code,
    },
    repr = _ represent not
]
-----
true
=====
repr const
-----
_ do _[
    _represent set _ import .function.represent,
    _ represent _ function {
        code : (.c : _i) : _i,
        constant : true
    }
]
-----
{
    constant : true,
    code : (.c : i) : i,
    prelude : .,
}
=====
repr primitive const
-----
_ do _[
    _represent set _ import .function.represent,
    _function.get_code set _ import .function.get_code,
    _type_code set _get_type which function.get_code : .,
    _repr set {
        constant : true,
        code : type_code,
    },
    repr = _ represent get_type
]
-----
true
=====
repr composite
-----
_ do _[
    _represent set _ import .function.represent,
    _repr set {
        constant : true,
        code : (.ctx : _in) : _in,
        prelude : {},
    },
    _new_repr set _ represent _ function repr,
    repr = new_repr
]
-----
true
=====
is_free free
-----
_ do _[
    _is_free set _ import .function.is_free,
    _function which is_free : .
]
-----
true
=====
is_free constant
-----
_ do _[
    _is_free set _ import .function.is_free,
    _get which is_free : .
]
-----
false
=====
is_free mutable
-----
_ do _[
    _is_free set _ import .function.is_free,
    _eval which is_free : .
]
-----
false
=====
is_constant free
-----
_ do _[
    _is_constant set _ import .function.is_constant,
    _function which is_constant : .
]
-----
true
=====
is_constant constant
-----
_ do _[
    _is_constant set _ import .function.is_constant,
    _get which is_constant : .
]
-----
true
=====
is_constant mutable
-----
_ do _[
    _is_constant set _ import .function.is_constant,
    _eval which is_constant : .
]
-----
false
=====
is_input_free free
-----
_ do _[
    _is_input_free set _ import .function.is_input_free,
    _abort which is_input_free : .
]
-----
true
=====
is_input_free aware
-----
_ do _[
    _is_input_free set _ import .function.is_input_free,
    _not which is_input_free : .
]
-----
false
=====
is_primitive primitive
-----
_ do _[
    _is_primitive set _ import .function.is_primitive,
    _not which is_primitive : .
]
-----
true
=====
is_primitive composite
-----
_ do _[
    _is_primitive set _ import .function.is_primitive,
    _f set _ function {code : (. : .) : .},
    _f which is_primitive : .
]
-----
false
=====
get_code
-----
_ do _[
    _function.get_code set _ import .function.get_code,
    _f set _ function {
        code : (.ctx : _i) : _i
    },
    _f which function.get_code : .
]
-----
(.ctx : i) : i
=====
prelude
-----
_ do _[
    _function.get_prelude set _ import .function.get_prelude,
    _any set _ import .value.any,
    _prelude set _ any .,
    _f set _ function {
        code : (. : .) : .,
        prelude : prelude
    },
    (_f which function.get_prelude : .) = prelude
]
-----
true
=====
recurse
-----
_ do _[
    _g set _ function {
        code : _((. : fi) : _ do _[
            _(f : i) represent fi,
            (i < 1) test _[
                0
            ] : _[
                i + f f i - 1
            ]
        ]),
        prelude : {
            do : do,
            represent : represent,
            < : <,
            test : test,
            + : +,
            - : -,
        },
    },
    g g 10
]
-----
55
