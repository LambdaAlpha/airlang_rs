get_steps
-----
_ do [
    .get_steps set _ import _resource.get_steps,
    .s1 set _ get_steps .,
    .s2 set _ get_steps .,
    s1 - s2
]
-----
9
=====
measure_steps 1
-----
_ do [
    .measure_steps set _ import _resource.measure_steps,
    (.v : .s) = _ measure_steps true,
    s
]
-----
1
=====
measure_steps ref + form + call
-----
_ do [
    .measure_steps set _ import _resource.measure_steps,
    .a set true,
    .b set false,
    (.v : .s) = _ measure_steps a and b,
    s
]
-----
6
=====
set_steps
-----
_ do [
    .new set _ import _config.new,
    .link.new set _ import _link.new,
    .set_steps set _ import _resource.set_steps,
    .where set _ import _config.where,
    .is_aborted set _ import _error.is_aborted,
    .prelude set _ link.new {
        do : do,
        set_steps : set_steps,
        + : +,
    },
    .c5 set _ new {
        _prelude : prelude,
    },
    .a5 set c5 where _ do [
        _ set_steps 5,
        1 + 1
    ],
    .c6 set _ new {
        _prelude : prelude,
    },
    .a6 set c6 where _ do [
        _ set_steps 6,
        1 + 1
    ],
    .c7 set _ new {
        _prelude : prelude,
    },
    .a7 set c7 where _ do [
        _ set_steps 7,
        1 + 1
    ],
    (.c5 which is_aborted : .) and (a6 == 2) and (a7 == 2)
]
-----
true
