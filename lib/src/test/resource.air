available_steps
-----
_ do [
    .steps set _ import _resource.get_available_steps,
    .s1 set _ steps .,
    .s2 set _ steps .,
    s1 - s2
]
-----
9
=====
measure_steps 1
-----
_ do [
    .measure_steps set _ import _resource.measure_steps,
    (.v : .s) = _ measure_steps true,
    s
]
-----
1
=====
measure_steps ref + form + call
-----
_ do [
    .measure_steps set _ import _resource.measure_steps,
    .a set true,
    .b set false,
    (.v : .s) = _ measure_steps a and b,
    s
]
-----
6
=====
limit_steps
-----
_ do [
    .limit_steps set _ import _resource.limit_steps,
    .a5 set 5 limit_steps _ data 1 + 1,
    .a6 set 6 limit_steps _ data 1 + 1,
    .a7 set 7 limit_steps _ data 1 + 1,
    (a5 == .) and (a6 == 2) and (a7 == 2)
]
-----
true
