; [
    f = function ! {
        body : the_input,
    },
    f ! not ! true
]
-----
false
=====
; [
    f = function ! {
        body : (. not) ! the_input
    },
    f ! not ! true
]
-----
true
=====
; [
    f = function ! {
        body : not ! the_input,
        prelude : . context {: not}
    },
    f ! not ! true
]
-----
true
=====
; [
    f = function ! {
        body : n ! a,
        input_name : a,
        prelude : . context {n : not}
    },
    f ! not ! true
]
-----
true
=====
; [
    f = function ! {
        body : the_input,
        input_mode : id,
    },
    f ! not ! true
]
-----
not true
=====
; [
    f = function ! {
        body : (. not) ! the_input,
        input_mode : eval,
    },
    f ! not ! true
]
-----
true
=====
; [
    f = function ! {
        body : the_input,
        input_mode : lazy,
    },
    f ! not ! true
]
-----
not true
=====
; [
    f = function ! {
        body : the_input,
        input_mode : lazy,
    },
    f ! . ! not ! true
]
-----
false
=====
; [
    f = function ! {
        body : the_input,
        input_mode : eval,
    },
    f {a : b}
]
-----
{. : .}
=====
; [
    f = function ! {
        body : the_input,
        input_mode : {pair : lazy : eval},
    },
    a f b
]
-----
a : .
=====
; [
    f = function ! {
        body : the_input,
        input_mode : {pair : lazy : eval, symbol : id},
    },
    [a f b, f a]
]
-----
[a : ., a]
=====
; [
    f = function ! {
        body : the_input,
        input_mode : id,
    },
    f ! not ! false
]
-----
not false
=====
; [
    f = function ! {
        body : the_input,
        input_mode : {symbol : id},
    },
    f ! not ! false
]
-----
true
=====
; [
    f = function ! {
        body : the_input,
        input_mode : {symbol : id},
    },
    f a
]
-----
a
=====
; [
    a = 1,
    f = function ! {
        body : the_input,
        input_mode : {symbol : id},
    },
    f [a]
]
-----
[1]
=====
; [
    a = 1,
    f = function ! {
        body : the_input,
        input_mode : {list : [eval, lazy, ... id, eval]},
    },
    f [a, a, a, a, a, a]
]
-----
[1, a, a, a, a, 1]
=====
; [
    f = function ! {
        body : the_input,
        input_mode : {list : all ! id},
    },
    f [a, b]
]
-----
[a, b]
=====
; [
    f = function ! {
        body : the_input,
        input_mode : {list : all ! id},
    },
    f ! not ! false
]
-----
true
=====
; [
    f = function ! {
        body : the_input,
        input_mode : {list : all ! {symbol : id}},
    },
    f [not true, b]
]
-----
[false, b]
=====
; [
    a = 1,
    f = function ! {
        body : the_input,
        input_mode : {map : {a : eval, b : id}},
    },
    f {a : a, b : a}
]
-----
{a : 1, b : a}
=====
; [
    f = function ! {
        body : the_input,
        input_mode : {map : id all id},
    },
    f {a : b}
]
-----
{a : b}
=====
; [
    f = function ! {
        body : the_input,
        input_mode : {map : id all id},
    },
    f (not true)
]
-----
false
=====
; [
    a = 1,
    f = function ! {
        body : the_input,
        input_mode : {map : lazy all eval},
    },
    f {: a}
]
-----
{a : 1}
=====
; [
    a = 1,
    f = function ! {
        body : the_input,
        input_mode : {call : eval ! id},
    },
    f (a a)
]
-----
1 a
=====
; [
    a = 1,
    f = function ! {
        body : the_input,
        input_mode : {call : {unit : id}},
    },
    ((f ! . ! a) == id ! . ! a) and (f ! not ! false) == true
]
-----
true
=====
; [
    a = id ! b,
    f = function ! {
        body : the_input,
        input_mode : {call : {symbol : id}},
    },
    ((f ! a ! a) == id ! b ! a) and (f ! not ! false) == true
]
-----
true
=====
; [
    a = 1,
    f = function ! {
        body : the_input,
        input_mode : {ask : eval ? id},
    },
    f ! a ? a
]
-----
1 ? a
=====
; [
    a = 1,
    f = function ! {
        body : the_input,
        input_mode : {ask : {unit : id}},
    },
    (f ! . ? a) == id ! . ? a
]
-----
true
=====
; [
    a = id ! b,
    f = function ! {
        body : the_input,
        input_mode : {ask : {symbol : id}},
    },
    ((f ! a ? a) == id ! b ? a) and (f ! . ? a) == id ! . ? b
]
-----
true
=====
; [
    a = 1,
    f = function ! {
        body : the_context | id ! a,
        context_access : free,
        prelude : . context {
            : |,
            : id,
        }
    },
    f .
]
-----
.
=====
; [
    a = 1,
    f = function ! {
        body : the_context | id ! a,
        context_access : constant,
        prelude : . context {
            : |,
            : id,
        }
    },
    f .
]
-----
1
=====
; [
    a = 1,
    f = function ! {
        body : the_context | id ! a,
        context_access : mutable,
        prelude : . context {
            : |,
            : id,
        }
    },
    f .
]
-----
1
=====
; [
    a = 1,
    f = function ! {
        body : the_context | id ! a,
        context_access : constant,
        prelude : . context {
            : |,
            : id,
        }
    },
    f .
]
-----
1
=====
; [
    f = function ! {
        body : the_context | '!' ! the_input = 2,
        input_mode : id,
        context_access : mutable,
        prelude : . context {
            : |,
            : =,
            : '!',
        }
    },
    a = 1,
    f a,
    a
]
-----
2
=====
; [
    f = function ! {
        body : the_context | '!' ! the_input = 2,
        input_mode : id,
        context_access : mutable,
        prelude : . context {
            : |,
            : =,
            : '!',
        }
    },
    (a . final) = 1,
    f a,
    a
]
-----
1
=====
; [
    f = function ! {
        body : the_context | '!' ! the_input = 2,
        input_mode : id,
        context_access : constant,
        prelude : . context {
            : |,
            : =,
            : '!',
        }
    },
    a = 1,
    f a,
    a
]
-----
1
=====
; [
    dispatcher = function ! {
        body : (the_input == (. .) ! id ! the_context) if (id c) : false,
        context_access : free,
        prelude : . context {
            : if,
            : ==,
            : id,
        },
    },
    f = function ! {
        body : the_context | !! ! a = 1,
        context_access : mutable,
        prelude : (. context {: dispatcher}) context {
            : =,
            : |,
            : !!,
            c : . context {},
        }
    },
    f .,
    a
]
-----
1
=====
function.represent ! function ! {body : the_input}
-----
{body : the_input}
=====
function.represent not
-----
{id : not}
=====
function.represent ! function ! {body : the_input, context_access : constant}
-----
{body : the_input, context_access : constant}
=====
function.represent ==
-----
{id : ==, context_access : constant, input_mode : {pair : {symbol : id} : {symbol : id}}}
=====
function.represent ! function ! {
    body : i,
    input_name : i,
    context_name : c,
    input_mode : {pair : eval : id},
    output_mode : {pair : lazy : lazy},
    context_access : mutable,
}
-----
{
    body : i,
    input_name : i,
    context_name : c,
    input_mode : {pair : eval : id},
    output_mode : {pair : lazy : lazy},
    context_access : mutable,
}
=====
function.context_access function
-----
free
=====
function.context_access function.context_access
-----
constant
=====
function.context_access eval
-----
mutable
=====
; [f = function ! {}, function.context_access f]
-----
free
=====
function.input_mode not
-----
eval
=====
function.input_mode eval
-----
id
=====
function.input_mode function.input_mode
-----
{symbol : id}
=====
; [f = function ! {}, function.input_mode f]
-----
eval
=====
; [
    f = function ! {
        input_mode : {pair : eval : lazy},
    },
    function.input_mode f
]
-----
{pair : eval : lazy}
=====
; [
    f = function ! {
        input_mode : {symbol : id},
    },
    function.input_mode f
]
-----
{symbol : id}
=====
; [
    f = function ! {
        input_mode : {call : eval ! id},
    },
    function.input_mode f
]
-----
{call : eval ! id}
=====
; [
    f = function ! {
        input_mode : {list : [eval, lazy, id, ... eval, id]},
    },
    function.input_mode f
]
-----
{list : [eval, lazy, id, ... eval, id]}
=====
; [
    f = function ! {
        input_mode : {list : all ! id},
    },
    function.input_mode f
]
-----
{list : all ! id}
=====
; [
    f = function ! {
        input_mode : {list : (all {symbol : id})},
    },
    function.input_mode f
]
-----
{list : all ! {symbol : id}}
=====
; [
    f = function ! {
        input_mode : {map : {a : eval, b : id}},
    },
    function.input_mode f
]
-----
{map : {a : eval, b : id}}
=====
; [
    f = function ! {
        input_mode : {map : id all id},
    },
    function.input_mode f
]
-----
{map : id all id}
=====
; [
    f = function ! {
        input_mode : {map : eval all id},
    },
    function.input_mode f
]
-----
{map : eval all id}
=====
; [
    f = function ! {
        input_mode : {map : {list : [{symbol : id}, {list : all ! id}]} all {list : all ! {map : id all id}}},
    },
    function.input_mode f
]
-----
{map : {list : [{symbol : id}, {list : all ! id}]} all {list : all ! {map : id all id}}}
=====
; [
    f = function ! {
        output_mode : {map : eval all id},
    },
    function.output_mode f
]
-----
{map : eval all id}
=====
function.is_primitive not
-----
true
=====
; [f = function ! {}, function.is_primitive f]
-----
false
=====
function.is_extension not
-----
false
=====
function.id not
-----
not
=====
; [neg = not, function.id neg]
-----
not
=====
; [f = function ! {}, function.id f]
-----
.
=====
; [f = function ! {body : the_input}, function.body f]
-----
the_input
=====
function.body not
-----
.
=====
; [
    ctx = . context {a : 1},
    f = function ! {prelude : ctx},
    (function.prelude f) == ctx
]
-----
true
=====
function.prelude not
-----
.
=====
; [f = function ! {}, function.input_name f]
-----
the_input
=====
function.input_name not
-----
.
=====
; [f = function ! {context_access : constant}, function.context_name f]
-----
the_context
=====
; [f = function ! {}, function.context_name f]
-----
.
=====
function.context_name not
-----
.
