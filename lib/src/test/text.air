length push
-----
_ do [
    any = _ import .value.any,
    length = _ import .text.length,
    push = _ import .text.push,
    t = _ any text,
    l = t which _ length .,
    t which _ push "a",
    l1 = t which _ length .,
    l1 == l + 1
]
-----
true
=====
length join
-----
_ do [
    any = _ import .value.any,
    length = _ import .text.length,
    join = _ import .text.join,
    a = _ any text,
    la = a which _ length .,
    b = _ any text,
    lb = b which _ length .,
    ab = . join [a, b],
    lab = ab which _ length .,
    lab == la + lb
]
-----
true
=====
length empty
-----
_ do [
    length = _ import .text.length,
    t = "",
    t which _ length .
]
-----
0
=====
length else
-----
_ do [
    length = _ import .text.length,
    t = "1",
    t which _ length .
]
-----
1
=====
push join
-----
_ do [
    any = _ import .value.any,
    push = _ import .text.push,
    join = _ import .text.join,
    a = _ any text,
    b = _ any text,
    t = a,
    t which _ push b,
    t1 = . join [a, b],
    t == t1
]
-----
true
=====
push
-----
_ do [
    push = _ import .text.push,
    a = "hello",
    b = "world",
    a which _ push " ",
    a which _ push b,
    a
]
-----
"hello world"
=====
join empty
-----
_ do [
    join = _ import .text.join,
    "" join ["1", "2"]
]
-----
"12"
=====
join unit
-----
_ do [
    join = _ import .text.join,
    a = "1",
    b = "2",
    c = "3",
    . join [a, b, c]
]
-----
"123"
=====
join no text
-----
_ do [
    join = _ import .text.join,
    . join []
]
-----
""
=====
join else
-----
_ do [
    join = _ import .text.join,
    "." join ["1", "2"]
]
-----
"1.2"
=====
into_utf8 from_utf8
-----
_ do [
    any = _ import .value.any,
    from_utf8 = _ import .text.from_utf8,
    into_utf8 = _ import .text.into_utf8,
    t = _ any text,
    t1 = _ from_utf8 _ into_utf8 t,
    t == t1
]
-----
true
=====
into_utf8
-----
_ do [
    into_utf8 = _ import .text.into_utf8,
    _ into_utf8 "abc"
]
-----
byte(616263)
=====
from_utf8
-----
_ do [
    from_utf8 = _ import .text.from_utf8,
    _ from_utf8 byte(616263)
]
-----
"abc"
