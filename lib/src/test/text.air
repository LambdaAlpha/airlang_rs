length push
-----
_ do [
    .any set _ import _value.any,
    .length set _ import _text.length,
    .push set _ import _text.push,
    .t set _ any _text,
    .l set t which _ length .,
    t which _ push "a",
    .l1 set t which _ length .,
    l1 == l + 1
]
-----
true
=====
length join
-----
_ do [
    .any set _ import _value.any,
    .length set _ import _text.length,
    .join set _ import _text.join,
    .a set _ any _text,
    .la set a which _ length .,
    .b set _ any _text,
    .lb set b which _ length .,
    .ab set . join [a, b],
    .lab set ab which _ length .,
    lab == la + lb
]
-----
true
=====
length empty
-----
_ do [
    .length set _ import _text.length,
    .t set "",
    t which _ length .
]
-----
0
=====
length else
-----
_ do [
    .length set _ import _text.length,
    .t set "1",
    t which _ length .
]
-----
1
=====
push join
-----
_ do [
    .any set _ import _value.any,
    .push set _ import _text.push,
    .join set _ import _text.join,
    .a set _ any _text,
    .b set _ any _text,
    .t set a,
    t which _ push b,
    .t1 set . join [a, b],
    t == t1
]
-----
true
=====
push
-----
_ do [
    .push set _ import _text.push,
    .a set "hello",
    .b set "world",
    a which _ push " ",
    a which _ push b,
    a
]
-----
"hello world"
=====
join empty
-----
_ do [
    .join set _ import _text.join,
    "" join ["1", "2"]
]
-----
"12"
=====
join unit
-----
_ do [
    .join set _ import _text.join,
    .a set "1",
    .b set "2",
    .c set "3",
    . join [a, b, c]
]
-----
"123"
=====
join no text
-----
_ do [
    .join set _ import _text.join,
    . join []
]
-----
""
=====
join else
-----
_ do [
    .join set _ import _text.join,
    "." join ["1", "2"]
]
-----
"1.2"
=====
into_utf8 from_utf8
-----
_ do [
    .any set _ import _value.any,
    .from_utf8 set _ import _text.from_utf8,
    .into_utf8 set _ import _text.into_utf8,
    .t set _ any _text,
    .t1 set _ from_utf8 _ into_utf8 t,
    t == t1
]
-----
true
=====
into_utf8
-----
_ do [
    .into_utf8 set _ import _text.into_utf8,
    _ into_utf8 "abc"
]
-----
byte(616263)
=====
from_utf8
-----
_ do [
    .from_utf8 set _ import _text.from_utf8,
    _ from_utf8 byte(616263)
]
-----
"abc"
