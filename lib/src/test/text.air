length push
-----
_ do [
    _any set _ import _value.any,
    _length set _ import _text.length,
    _push set _ import _text.push,
    _t set _ any _text,
    _l set t which _ length .,
    t which _ push "a",
    _l1 set t which _ length .,
    l1 == l + 1
]
-----
true
=====
length join
-----
_ do [
    _any set _ import _value.any,
    _length set _ import _text.length,
    _join set _ import _text.join,
    _a set _ any _text,
    _la set a which _ length .,
    _b set _ any _text,
    _lb set b which _ length .,
    _ab set . join [a, b],
    _lab set ab which _ length .,
    lab == la + lb
]
-----
true
=====
length empty
-----
_ do [
    _length set _ import _text.length,
    _t set "",
    t which _ length .
]
-----
0
=====
length else
-----
_ do [
    _length set _ import _text.length,
    _t set "1",
    t which _ length .
]
-----
1
=====
push join
-----
_ do [
    _any set _ import _value.any,
    _push set _ import _text.push,
    _join set _ import _text.join,
    _a set _ any _text,
    _b set _ any _text,
    _t set a,
    t which _ push b,
    _t1 set . join [a, b],
    t == t1
]
-----
true
=====
push
-----
_ do [
    _push set _ import _text.push,
    _a set "hello",
    _b set "world",
    a which _ push " ",
    a which _ push b,
    a
]
-----
"hello world"
=====
join empty
-----
_ do [
    _join set _ import _text.join,
    "" join ["1", "2"]
]
-----
"12"
=====
join unit
-----
_ do [
    _join set _ import _text.join,
    _a set "1",
    _b set "2",
    _c set "3",
    . join [a, b, c]
]
-----
"123"
=====
join no text
-----
_ do [
    _join set _ import _text.join,
    . join []
]
-----
""
=====
join else
-----
_ do [
    _join set _ import _text.join,
    "." join ["1", "2"]
]
-----
"1.2"
=====
into_utf8 from_utf8
-----
_ do [
    _any set _ import _value.any,
    _from_utf8 set _ import _text.from_utf8,
    _into_utf8 set _ import _text.into_utf8,
    _t set _ any _text,
    _t1 set _ from_utf8 _ into_utf8 t,
    t == t1
]
-----
true
=====
into_utf8
-----
_ do [
    _into_utf8 set _ import _text.into_utf8,
    _ into_utf8 "abc"
]
-----
byte(616263)
=====
from_utf8
-----
_ do [
    _from_utf8 set _ import _text.from_utf8,
    _ from_utf8 byte(616263)
]
-----
"abc"
