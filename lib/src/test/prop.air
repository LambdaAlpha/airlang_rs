;[
    p = (proposition {
        function : not,
        input : 1,
        output : 2,
    }),
    proposition.input p
]
-----
1
=====
;[
    p = (proposition {
        function : not,
        input : 1,
        output : 2,
    }),
    proposition.output p
]
-----
2
=====
;[
    p = (proposition {
        function : ===,
        input : 1,
        output : 1,
        context : (' context {a : 2}),
    }),
    ctx = (proposition.before p),
    [ctx] do (read a)
]
-----
2
=====
;[
    p = (proposition {
        function : ===,
        input : 1,
        output : 1,
        context : (' context {a : 2}),
    }),
    ctx = (proposition.after p),
    [ctx] do (read a)
]
-----
2
=====
;[
    p = (proposition {
        function : =,
        input : 1,
        output : 1,
        before : (' context {a : 2}),
        after : (' context {a : 3}),
    }),
    ctx = (proposition.before p),
    [ctx] do (read a)
]
-----
2
=====
;[
    p = (proposition {
        function : =,
        input : 1,
        output : 1,
        before : (' context {a : 2}),
        after : (' context {a : 3}),
    }),
    ctx = (proposition.after p),
    [ctx] do (read a)
]
-----
3
=====
;[
    p = (proposition {
        function : not,
        input : 1,
        output : 2,
    }),
    (proposition.function p) -== not
]
-----
't
=====
;[
    $map = (function {
        body : input,
        input_mode : (' map_items 't),
    }),
    m = ($map {
        function : not,
        input : 't,
        output : 'f,
    }),
    m ==- (proposition.represent (proposition m))
]
-----
't
=====
;[
    $map = (function {
        body : input,
        input_mode : (' map_items 't),
    }),
    m = ($map {
        function : ===,
        input : 't,
        output : 'f,
        context : (' context {}),
    }),
    m ==- (proposition.represent (proposition m))
]
-----
't
=====
;[
    $map = (function {
        body : input,
        input_mode : (' map_items 't),
    }),
    m = ($map {
        function : =,
        input : 't,
        output : 'f,
        before : (' context {}),
        after : (' context {}),
    }),
    m ==- (proposition.represent (proposition m))
]
-----
't
=====
;[
    m = (proposition.represent (theorem {
        function : not,
        input : 't,
    })),
    $map = (function {
        body : input,
        input_mode : (' map_items 't),
    }),
    expected = ($map {
        function : not,
        input : 't,
        output : 'f,
        truth : 't,
    }),
    m === expected
]
-----
't
=====
;[
    m = (proposition.represent (theorem {
        function : ===,
        input : (' (a : b)),
        context : (' context {a : 1, b : 2}),
    })),
    $map = (function {
        body : input,
        input_mode : (' map_items 't),
    }),
    expected = ($map {
        function : ===,
        input : (' (a : b)),
        output : 'f,
        context : (' context {a : 1, b : 2}),
        truth : 't,
    }),
    m === expected
]
-----
't
=====
;[
    m = (proposition.represent (theorem {
        function : =,
        input : ((' a) : 1),
        before : (' context {}),
    })),
    $map = (function {
        body : input,
        input_mode : (' map_items 't),
    }),
    expected = ($map {
        function : =,
        input : ((' a) : 1),
        output : ',
        before : (' context {}),
        after : (' context {a : 1}),
        truth : 't,
    }),
    m === expected
]
-----
't
=====
;[
    p = (proposition {
        function : not,
        input : 1,
        output : 2,
    }),
    ((type_of p) -=- (' proposition)) and ((proposition.truth p) -=- ')
]
-----
't
