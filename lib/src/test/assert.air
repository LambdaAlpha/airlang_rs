; [
    p = assert ! {
        function : not,
        input : 1,
        output : 2,
    },
    assert.input .p
]
-----
1
=====
; [
    p = assert ! {
        function : not,
        input : 1,
        output : 2,
    },
    assert.output .p
]
-----
2
=====
; [
    p = assert ! {
        function : not,
        input : 1,
        output : 2,
    },
    (assert.function .p) == .not
]
-----
true
=====
; [
    p = assert ! {
        function : "not",
        input : 1,
        output : 2,
    },
    (assert.function .p) == "not"
]
-----
true
=====
; [
    map_mode = function ! {
        body : the_input,
        input_mode : {map : id : eval},
    },
    m = map_mode ! {
        function : not,
        input : true,
        output : false,
    },
    .m == assert.represent ! assert ! m
]
-----
true
=====
; [
    m = assert.represent ! assert.verified ! not '!' true,
    map_mode = function ! {
        body : the_input,
        input_mode : {map : id : eval},
    },
    expected = map_mode ! {
        function : not,
        input : true,
        output : false,
        verified : true,
    },
    .m == .expected
]
-----
true
=====
; [
    p = assert ! {
        function : not,
        input : 1,
        output : 2,
    },
    tp = type_of ! .p,
    (.tp == ..assert) and (assert.is_verified .p) == false
]
-----
true
