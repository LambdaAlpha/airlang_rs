; [
    p = assert.verified ! not '!' (not false),
    assert.input .p
]
-----
true
=====
; [
    p = assert.verified ! not '!' true,
    assert.output .p
]
-----
false
=====
; [
    p = assert.verified ! not '!' true,
    (assert.function .p) == .not
]
-----
true
=====
; [
    p = assert.verified ! not '!' true,
    assert.is_verified .p
]
-----
true
