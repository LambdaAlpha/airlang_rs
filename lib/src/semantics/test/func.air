;[
    f = (function (value {
        body : input,
    })),
    f (not 't)
]
# ---
'f
# ===
;[
    f = (function (quote {
        body : (('t not) input)
    })),
    f (not 't)
]
# ---
't
# ===
;[
    f = (function {
        body : (value (not input)),
        context : (context {map : {: not}})
    }),
    f (not 't)
]
# ---
't
# ===
;[
    f = (function {
        body : (value (n a)),
        input_name : (value a),
        context : (context {map : {n : not}})
    }),
    f (not 't)
]
# ---
't
# ===
;[
    f = (function (value {
        body : input,
        eval_mode : value,
    })),
    f (not 't)
]
# ---
not 't
# ===
;[
    f = (function (quote {
        body : (('t not) input),
        eval_mode : eval,
    })),
    f (not 't)
]
# ---
't
# ===
;[
    f = (function (value {
        body : input,
        eval_mode : quote,
    })),
    f (not 't)
]
# ---
not 't
# ===
;[
    f = (function (value {
        body : input,
        eval_mode : quote,
    })),
    f ('t (not 't))
]
# ---
'f
# ===
;[
    f = (function (value {
        body : input,
        eval_mode : eval,
    })),
    f {a : b}
]
# ---
{a : '}
# ===
;[
    f = (function (value {
        body : input,
        pair_eval_mode : (quote : eval),
    })),
    a f b
]
# ---
a : '
# ===
;[
    f = (function (value {
        body : input,
        pair_eval_mode : (quote : eval),
        eval_mode : value
    })),
    f (not 'f)
]
# ---
not 'f
# ===
;[
    f = (function (quote {
        body : (;[[] set_super caller, eval_2 input]),
        eval_mode : value,
        caller_access : mutable,
        context : ('t (context {map : {
            : ;,
            : eval_2,
            : set_super,
            : load,
        }}))
    })),
    a = 1,
    f a
]
# ---
1
# ===
;[
    a = 1,
    f = (function (quote {
        body : (caller eval_constant a),
        caller_access : free,
        context : ('t (context {map : {
            : eval_constant,
        }}))
    })),
    f '
]
# ---
'
# ===
;[
    a = 1,
    f = (function (quote {
        body : (caller eval_constant a),
        caller_access : constant,
        context : ('t (context {map : {
            : eval_constant,
        }}))
    })),
    f '
]
# ---
1
# ===
;[
    a = 1,
    f = (function (quote {
        body : (caller eval_constant a),
        caller_access : mutable,
        context : ('t (context {map : {
            : eval_constant,
        }}))
    })),
    f '
]
# ---
1
# ===
;[
    a = 1,
    f = (function (quote {
        body : (caller eval_mutable a),
        caller_access : constant,
        context : ('t (context {map : {
            : eval_mutable,
        }}))
    })),
    f '
]
# ---
1
# ===
;[
    f = (function (quote {
        body : (caller eval_mutable (('f ('t input)) = 2)),
        eval_mode : value,
        caller_access : mutable,
        context : ('t (context {map : {
            : eval_mutable,
        }}))
    })),
    a = 1,
    f a,
    a
]
# ---
2
# ===
;[
    f = (function (quote {
        body : (caller eval_mutable (('f ('t input)) = 2)),
        eval_mode : value,
        caller_access : mutable,
        context : ('t (context {map : {
            : eval_mutable,
        }}))
    })),
    a =final 1,
    f a,
    a
]
# ---
1
# ===
;[
    f = (function (quote {
        body : (caller eval_mutable (('f ('t input)) = 2)),
        eval_mode : value,
        caller_access : constant,
        context : ('t (context {map : {
            : eval_mutable,
        }}))
    })),
    a = 1,
    f a,
    a
]
# ---
1
# ===
function_represent (function (quote {body : input}))
# ---
{body : input}
# ===
function_represent not
# ---
{id : not}
# ===
function_represent (function (value {body : input, caller_access : constant}))
# ---
{body : input, caller_access : constant}
# ===
function_represent ==
# ---
{id : ==, caller_access : constant, eval_mode : value, pair_eval_mode : (quote : quote)}
# ===
function_represent (function (value {
    body : i,
    input_name : i,
    caller_name : c,
    eval_mode : value,
    pair_eval_mode : (quote : quote),
    caller_access : mutable,
}))
# ---
{
    body : i,
    input_name : i,
    caller_name : c,
    eval_mode : value,
    pair_eval_mode : (quote : quote),
    caller_access : mutable,
}
# ===
function_caller_access function
# ---
free
# ===
function_caller_access function_caller_access
# ---
constant
# ===
function_caller_access eval
# ---
mutable
# ===
;[f = (function (value {body : input})), function_caller_access f]
# ---
free
# ===
function_eval_mode not
# ---
eval
# ===
function_eval_mode eval
# ---
value
# ===
function_eval_mode function_eval_mode
# ---
quote
# ===
;[f = (function (value {body : input})), function_eval_mode f]
# ---
eval
# ===
function_pair_eval_mode and
# ---
eval : eval
# ===
function_pair_eval_mode not
# ---
eval : eval
# ===
function_is_primitive not
# ---
't
# ===
;[f = (function (value {body : input})), function_is_primitive f]
# ---
'f
# ===
function_id not
# ---
not
# ===
;[neg = not, function_id neg]
# ---
not
# ===
;[f = (function (value {body : input})), function_id f]
# ---
'
# ===
;[f = (function (value {body : input})), function_body f]
# ---
input
# ===
function_body not
# ---
'
# ===
;[
    ctx = (context {map : {a : 1}}),
    f = (function {body : (value input), context : ctx}),
    ('t (function_context f)) == ctx
]
# ---
't
# ===
function_context not
# ---
'
# ===
;[f = (function (value {body : input})), function_input_name f]
# ---
input
# ===
function_input_name not
# ---
'
# ===
;[f = (function (value {body : input, caller_access : constant})), function_caller_name f]
# ---
caller
# ===
;[f = (function (value {body : input})), function_caller_name f]
# ---
'
# ===
function_caller_name not
# ---
'
