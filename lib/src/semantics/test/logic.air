;[
    p = (proposition {
        function : not,
        input : 1,
        output : 2,
    }),
    !input p
]
# ---
1
# ===
;[
    p = (proposition {
        function : not,
        input : 1,
        output : 2,
    }),
    !output p
]
# ---
2
# ===
;[
    p = (proposition {
        function : ==,
        input : 1,
        output : 1,
        context : (context {var : {a : 2}}),
    }),
    ctx = (!before p),
    ctx eval_const a
]
# ---
2
# ===
;[
    p = (proposition {
        function : ==,
        input : 1,
        output : 1,
        context : (context {var : {a : 2}}),
    }),
    ctx = (!after p),
    ctx eval_const a
]
# ---
2
# ===
;[
    p = (proposition {
        function : =,
        input : 1,
        output : 1,
        before : (context {var : {a : 2}}),
        after : (context {var : {a : 3}}),
    }),
    ctx = (!before p),
    ctx eval_const a
]
# ---
2
# ===
;[
    p = (proposition {
        function : =,
        input : 1,
        output : 1,
        before : (context {var : {a : 2}}),
        after : (context {var : {a : 3}}),
    }),
    ctx = (!after p),
    ctx eval_const a
]
# ---
3
# ===
;[
    p = (proposition {
        function : not,
        input : 1,
        output : 2,
    }),
    (!function p) == not
]
# ---
't
# ===
;[
    t = (theorem {
        function : not,
        input : 't,
    }),
    !input t
]
# ---
't
# ===
;[
    t = (theorem {
        function : not,
        input : 't,
    }),
    !output t
]
# ---
'f
# ===
;[
    t = (theorem {
        function : not,
        input : 't,
    }),
    (!function t) == not
]
# ---
't
# ===
;[
    t = (theorem {
        function : not,
        input : 't,
    }),
    is_true t
]
# ---
't
# ===
;[
    t = (theorem {
        function : eval,
        input : (quote ((' not) 't)),
        context : (context {}),
    }),
    !output t
]
# ---
'f
# ===
;[
    t = (theorem {
        function : ==,
        input : (value (a : b)),
        context : (context {
            var : {
                a : 1,
                b : 2,
            }
        })
    }),
    !output t
]
# ---
'f
# ===
;[
    t = (theorem {
        function : =,
        input : (value (a : 1)),
        context : (context {})
    }),
    ctx = (!before t),
    ctx eval_const ((' is_null) a)
]
# ---
't
# ===
;[
    t = (theorem {
        function : =,
        input : (value (a : 1)),
        context : (context {})
    }),
    ctx = (!after t),
    ctx eval_const ((' is_null) a)
]
# ---
'f
# ===
;[
    p = (proposition {
        function : not,
        input : 't,
        output : 'f,
    }),
    t = (prove p),
    is_true t
]
# ---
't
# ===
;[
    p = (proposition {
        function : not,
        input : 't,
        output : 't,
    }),
    t = (prove p),
    is_true t
]
# ---
'f
