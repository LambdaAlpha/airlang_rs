parse "(1, 2)"
# ---
(1, 2)
# ===
;(a = 1, parse "a")
# ---
a
# ===
parse [stringify (1, 2)]
# ---
(1, 2)
# ===
stringify "1"
# ---
"\"1\""
# ===
val a
# ---
a
# ===
;(a = 1, eval [val a])
# ---
1
# ===
;(
    f = [func{
        eval: [n [eval_in_ctx [c:a]]],
        input_name: a,
        caller_name: c,
        constants: {n: not, eval_in_ctx}
    }],
    f 't
)
# ---
'f
# ===
;(
    f = [func{
        eval: [n [eval_in_ctx [c:a]]],
        input_name: a,
        caller_name: c,
        constants: {n: not, eval_in_ctx}
    }],
    f [not 't]
)
# ---
't
# ===
;(
    a = 'f,
    f = [func{
        eval: [a == b],
        input_name: b,
        constants: {
            ==,
            a
        }
    }],
    f 't
)
# ---
'f
# ===
;(
    f = [func{
        eval: [;(a = 1, eval code)],
        input_name: code,
        constants: {
            ;,
            =,
            eval,
        }
    }],
    f a
)
# ---
1
