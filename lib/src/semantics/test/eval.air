parse "(1, 2)"
# ---
(1, 2)
# ===
;(a = 1, parse "a")
# ---
a
# ===
parse [stringify (1, 2)]
# ---
(1, 2)
# ===
stringify "1"
# ---
"\"1\""
# ===
$0 a
# ---
a
# ===
$ [not 'f]
# ---
't
# ===
$\ (a, f a, a: b, f? a, {a: b})
# ---
(a, f a, a: b, f? a, {a: b})
# ===
$\ {a: b, c: [\ [not 'f]]}
# ---
{a: b, c: 't}
# ===
$\ (a, b: (\ [not 't]))
# ---
(a, b: ('f))
# ===
$map {a, \ [not 't]: c, not 'f}
# ---
{a: ', 'f: ', not 'f: 't}
# ===
;(a = 1, $$ [$0 a])
# ---
1
# ===
[$0 a] $$ [$0 b]
# ---
': '
# ===
;(
    f = [func{
        body: [n [c $@ a]],
        input: a,
        caller: c,
        const: {n: not, $@}
    }],
    f 't
)
# ---
'f
# ===
;(
    f = [func{
        body: [n [c $@ a]],
        input: a,
        caller: c,
        const: {n: not, $@}
    }],
    f [not 't]
)
# ---
't
# ===
;(
    f = [func{
        body: [not [caller $@ input]],
        const: {not, $@}
    }],
    f [not 't]
)
# ---
't
# ===
;(
    f = [func{
        body: _,
        input: _,
        caller: _,
        const: {}
    }],
    f [not 't]
)
# ---
'
# ===
;(
    a = 'f,
    f = [func{
        body: [a == input],
        const: {
            ==,
            a
        }
    }],
    f 't
)
# ---
'f
# ===
;(
    f = [func{
        body: [;(a = 1, $$ input)],
        const: {
            ;,
            =,
            $$,
        }
    }],
    f a
)
# ---
1
# ===
't . not
# ---
'f
# ===
't . not . not and 't
# ---
't
