parse "(1, 2)"
# ---
(1, 2)
# ===
;(a = 1, parse "a")
# ---
a
# ===
parse [stringify (1, 2)]
# ---
(1, 2)
# ===
stringify "1"
# ---
"\"1\""
# ===
val a
# ---
a
# ===
;(a = 1, eval [val a])
# ---
1
# ===
;(
    f = [func{
        body: [n [c eval a]],
        input: a,
        caller: c,
        const: {n: not, eval}
    }],
    f 't
)
# ---
'f
# ===
;(
    f = [func{
        body: [n [c eval a]],
        input: a,
        caller: c,
        const: {n: not, eval}
    }],
    f [not 't]
)
# ---
't
# ===
;(
    f = [func{
        body: [not [caller eval input]],
        const: {not, eval}
    }],
    f [not 't]
)
# ---
't
# ===
;(
    f = [func{
        body: _,
        input: _,
        caller: _,
        const: {}
    }],
    f [not 't]
)
# ---
'
# ===
;(
    a = 'f,
    f = [func{
        body: [a == input],
        const: {
            ==,
            a
        }
    }],
    f 't
)
# ---
'f
# ===
;(
    f = [func{
        body: [;(a = 1, eval input)],
        const: {
            ;,
            =,
            eval,
        }
    }],
    f a
)
# ---
1
