_ do _[
    _prelude set _ import .prelude,
    _link.which set _ import .link.which,
    
    prelude link.which set : _empty : empty,
    prelude link.which set : _return : return,
    prelude link.which set : _continue : continue,
    prelude link.which set : _break : break,
]
