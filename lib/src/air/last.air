_ do _[
    _prelude set _ import .prelude,
    _link_which set _ import .link.which,
    
    prelude link_which set : _empty : empty,
    prelude link_which set : _return : return,
    prelude link_which set : _continue : continue,
    prelude link_which set : _break : break,
]
