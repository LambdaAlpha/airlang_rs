_ do _[
    _prelude set _ import .prelude,
    _map.set set _ import .map.set.link.let,
    
    prelude map.set _empty : empty,
    prelude map.set _return : return,
    prelude map.set _continue : continue,
    prelude map.set _break : break,
    
    prelude map.set _type : _ import .value.get_type.context.let,
]
