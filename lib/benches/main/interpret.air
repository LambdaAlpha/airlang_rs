_("bench interpret")

_ do [
    _% set _ import _integer.remainder,
    _f set _ function {
        code : _n : _ do [
            __sum set 0,
            _n iterate _i : [
                (0 == _i % 3) __continue .,
                __sum set _sum + _i,
            ],
            _sum
        ],
        context_access : _free,
    },
    _ f 100
]
