_("bench interpret")

_ do [
    % = _ import .integer.remainder,
    f = _ function {
        code : .n : _ do [
            .sum = 0,
            .n iterate .i : [
                (0 == .i % 3) ..continue .,
                .sum = .sum + .i,
            ],
            .sum
        ],
        context_access : .free,
    },
    _ f 100
]
